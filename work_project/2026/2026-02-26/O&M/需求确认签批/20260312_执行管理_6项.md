# 执行管理模块 - 需求修改方案确认表

> **文件编号**：20260312_执行管理_6项
> **创建日期**：2026-03-12
> **模块名称**：执行管理
> **改动数量**：6项
> **修改范围**：demo_project/pages.js + demo_project/app.js（局部最小化修改）

---

## 改动1 — 新增红色/橙色超期警戒状态

**【改动编号】**：REQ-EXEC-001

**【修改前】**：
```javascript
// pages.js 第9-14行
<div class="legend-row">
    <span class="status-light gray"></span> 未开始：当前时间≤实际开始时间或无实际开始时间 &nbsp;|&nbsp;
    <span class="status-light blue"></span> 进行中：实际开始≤当前时间＜实际结束 &nbsp;|&nbsp;
    <span class="status-light green"></span> 已完成：当前时间≥实际结束时间
</div>
```

**【修改后】**：
```javascript
// pages.js 第9-14行
<div class="legend-row">
    <span class="status-light gray"></span> 未开始：当前时间≤实际开始时间或无实际开始时间 &nbsp;|&nbsp;
    <span class="status-light blue"></span> 进行中：实际开始≤当前时间＜实际结束 &nbsp;|&nbsp;
    <span class="status-light green"></span> 已完成：当前时间≥实际结束时间 &nbsp;|&nbsp;
    <span class="status-light red"></span> 超期警戒：执行中但计划结束时间已过
</div>
```

同时在示例数据中，将第1行（进行中且计划结束时间已过）的状态灯改为红色：
```javascript
// 第54行
<td><span class="status-light red" title="超期警戒"></span></td>
```

**【您的确认】**：同意

---

## 改动2 — 计划结束时间列增加超期天数排序功能

**【改动编号】**：REQ-EXEC-002

**【修改前】**：
```javascript
// pages.js 第40行
<th>计划结束</th>
```

**【修改后】**：
```javascript
// pages.js 第40行
<th>计划结束 <button class="sort-btn" onclick="sortByOverdueDays()" title="按超期天数排序">⇅</button></th>
```

同时在 app.js 中新增排序函数（在文件末尾追加，不影响其他功能）：
```javascript
// app.js 新增函数
function sortByOverdueDays() {
    showToast('已按超期天数排序（演示）');
    // 实际项目中需要重新请求后端数据并排序
}
```

**【您的确认】**：同意

---

## 改动3 — 新增"派单变更状态"列

**【改动编号】**：REQ-EXEC-003

**【修改前】**：
```javascript
// pages.js 第38-44行（表头）
<th>#</th><th>监理编号</th><th>合同负责人</th>...<th>预检会</th>
<th style="min-width:80px">操作</th>
```

**【修改后】**：
```javascript
// pages.js 第38-44行（表头）
<th>#</th><th>监理编号</th><th>合同负责人</th>...<th>预检会</th>
<th>派单变更</th>
<th style="min-width:80px">操作</th>
```

同时在示例数据中增加对应的 `<td>` 列：
```javascript
// 第56行后增加
<td><span class="tag tag-gray" onclick="showChangeLog('PD-20260115-01')" style="cursor:pointer">查看变更</span></td>

// 第68行后增加
<td>/</td>

// 第80行后增加
<td>/</td>
```

在 app.js 中新增查看变更明细函数：
```javascript
// app.js 新增函数
function showChangeLog(pd) {
    openModal('派单变更记录 - ' + pd, `
        <div style="font-size:12px;color:var(--text-muted);margin-bottom:12px">
            显示该派单的历史变更记录（换人、改时间等）
        </div>
        <table class="data-table" style="font-size:12px">
            <thead><tr><th>变更时间</th><th>变更类型</th><th>变更内容</th><th>操作人</th></tr></thead>
            <tbody>
                <tr><td>2026-02-15</td><td>人员变更</td><td>辅助人员：李四 → 陈伟</td><td>周磊</td></tr>
                <tr><td>2026-01-25</td><td>时间调整</td><td>计划结束：2026-03-15 → 2026-02-28</td><td>李明</td></tr>
            </tbody>
        </table>
    `);
}
```

**【您的确认】**：同意

---

## 改动4 — 非PECMS项目技术文件标红警示

**【改动编号】**：REQ-EXEC-004

**【修改前】**：
```javascript
// pages.js 第80行（第3行数据的技术文件列）
<td>❌否</td><td>❌否</td><td><span class="tag tag-green">低</span></td><td>❌否</td><td>❌否</td><td>❌否</td><td>❌否</td>
```

**【修改后】**：
```javascript
// pages.js 第80行
<td class="text-danger" style="font-weight:600">❌否</td>
<td class="text-danger" style="font-weight:600">❌否</td>
<td><span class="tag tag-green">低</span></td>
<td class="text-danger" style="font-weight:600">❌否</td>
<td class="text-danger" style="font-weight:600">❌否</td>
<td class="text-danger" style="font-weight:600">❌否</td>
<td class="text-danger" style="font-weight:600">❌否</td>
```

**说明**：第3行数据"是否使用监造平台"为"是"，但假设其上的平台非PECMS，因此技术文件字段为空时标红。

**【您的确认】**：同意

---

## 改动5 — 合同编号点击弹窗展示详情

**【改动编号】**：REQ-EXEC-005

**【修改前】**：
```javascript
// pages.js 第48行
<td>CI-2026-001</td>
```

**【修改后】**：
```javascript
// pages.js 第48行
<td><a href="javascript:void(0)" onclick="showContractDetail('CI-2026-001')" style="color:var(--primary);text-decoration:underline">CI-2026-001</a></td>
```

同样修改第60行和第72行的合同编号。

在 app.js 中新增合同详情弹窗函数：
```javascript
// app.js 新增函数
function showContractDetail(ci) {
    openWideModal('合同详情 - ' + ci, `
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;font-size:13px">
            <div><strong>合同编号：</strong>${ci}</div>
            <div><strong>合同名称：</strong>石化换热器监造服务合同</div>
            <div><strong>委托方：</strong>中石化炼化工程集团</div>
            <div><strong>制造方：</strong>东方锅炉厂</div>
            <div><strong>合同金额：</strong>500,000 元</div>
            <div><strong>项目经理：</strong>李明</div>
            <div><strong>合同类型：</strong>开口合同</div>
            <div><strong>签订时间：</strong>2025-12-20</div>
        </div>
        <div style="margin-top:16px;padding:12px;background:var(--bg-secondary);border-radius:4px;font-size:12px">
            <strong>备注：</strong>此处展示合同详细信息，数据来源于O&M系统内部，不跳转PECMS。
        </div>
    `);
}
```

**【您的确认】**：同意

---

## 改动6 — 修正用工时间显示规则

**【改动编号】**：REQ-EXEC-006

**【修改前】**：
```javascript
// pages.js 第78行（第3行数据，未开始状态）
<td>—</td>
```

**【修改后】**：
```javascript
// pages.js 第78行
<td></td>
```

**说明**：SRS要求未开始状态显示"空格"而非"—"，改为空的 `<td></td>`。

**【您的确认】**：同意

---

## 汇总总览表

| 改动编号 | 改动位置 | 改动类型 | 改动摘要 | 您的确认 |
|---------|---------|---------|---------|---------|
| REQ-EXEC-001 | pages.js + 示例数据 | 新增 | 增加红色超期警戒状态灯及说明 |  |
| REQ-EXEC-002 | pages.js + app.js | 新增 | 计划结束时间列增加排序按钮和函数 |  |
| REQ-EXEC-003 | pages.js + app.js | 新增 | 新增"派单变更状态"列及查看变更明细弹窗 |  |
| REQ-EXEC-004 | pages.js | 修改 | 非PECMS项目技术文件字段为空时标红 |  |
| REQ-EXEC-005 | pages.js + app.js | 新增 | 合同编号可点击，弹窗展示详情 |  |
| REQ-EXEC-006 | pages.js | 修改 | 未开始状态用工时间显示空格而非"—" |  |

---

## 修改原则说明

✅ **最小化修改**：只修改执行管理模块相关代码
✅ **不影响其他模块**：新增函数独立，不修改公共函数
✅ **局部追加**：在 app.js 末尾追加新函数，不插入中间
✅ **保持风格一致**：使用现有的 CSS 类和交互模式

---

**请您在每项【您的确认】处填写：**
- ✅ 同意
- ❌ 不做
- 📝 有修改（括号内说明）

确认后我将执行第二步：修改代码 + 更新SRS文档 + 记录日志。
