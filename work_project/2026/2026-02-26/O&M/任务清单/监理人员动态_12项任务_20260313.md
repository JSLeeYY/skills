# 监理人员动态模块 - 功能补充任务清单

**创建日期**：2026-03-13
**模块名称**：监理人员动态
**总任务数**：12项
**优先级**：🔴 高 | 🟡 中 | 🟢 低

---

## 任务概览

| 任务ID | 任务名称 | 优先级 | 状态 | 预计工作量 |
|--------|---------|--------|------|-----------|
| T1 | 补充一级页面筛选条件（6个字段） | 🔴 高 | ⏳ 待开始 | 30分钟 |
| T2 | 添加角色切换功能 | 🟡 中 | ⏳ 待开始 | 20分钟 |
| T3 | 添加导出功能按钮 | 🟢 低 | ⏳ 待开始 | 10分钟 |
| T4 | 完善分页控件 | 🟢 低 | ⏳ 待开始 | 15分钟 |
| T5 | 实现二级页面 - 页面结构 | 🔴 高 | ⏳ 待开始 | 40分钟 |
| T6 | 实现二级页面 - 筛选条件 | 🔴 高 | ⏳ 待开始 | 30分钟 |
| T7 | 实现二级页面 - 数据表格 | 🔴 高 | ⏳ 待开始 | 30分钟 |
| T8 | 实现二级页面 - 操作按钮 | 🔴 高 | ⏳ 待开始 | 15分钟 |
| T9 | 实现三级页面 - 弹窗结构 | 🟡 中 | ⏳ 待开始 | 30分钟 |
| T10 | 实现三级页面 - 工时明细表格 | 🟡 中 | ⏳ 待开始 | 25分钟 |
| T11 | 实现三级页面 - 筛选条件 | 🟡 中 | ⏳ 待开始 | 20分钟 |
| T12 | 实现三级页面 - 工时日历 | 🟡 中 | ⏳ 待开始 | 60分钟 |

**总预计工作量**：约 5.5 小时

---

## 详细任务说明

### 🔴 T1: 补充一级页面筛选条件（6个字段）

**任务描述**：
当前一级页面只有6个筛选字段，需要补充剩余6个字段，使筛选条件完整。

**当前状态**：
- ✅ 已有：姓名、性别、民族、政治面貌、文化程度、职称
- ❌ 缺少：出生年月、年龄、入职日期、毕业院校、专业、监理师证书

**实现要点**：
1. 在现有筛选区域下方添加第二行筛选条件
2. 添加6个新的筛选字段：
   - 出生年月：日期范围选择器（开始月份 ~ 结束月份）
   - 年龄：数字范围输入框（最小年龄 ~ 最大年龄）
   - 入职日期：日期范围选择器
   - 毕业院校：文本输入框，支持模糊搜索
   - 专业：文本输入框，支持模糊搜索
   - 监理师证书：文本输入框，支持模糊搜索
3. 保持与第一行筛选条件相同的样式和布局

**代码位置**：
- 文件：`pages.js`
- 行号：537-549（筛选区域）

**注意事项**：
- ⚠️ 不要修改现有的6个筛选字段
- ⚠️ 确保新增的筛选条件不影响其他页面的筛选组件

---

### 🟡 T2: 添加角色切换功能

**任务描述**：
添加角色选择器，允许用户切换角色以演示不同角色的数据权限过滤。

**实现要点**：
1. 在页面标题行的角色徽章位置，将静态文字改为下拉选择器
2. 角色选项：
   - 项目总监
   - 项目经理
   - 总监代表
   - 监理人员
   - 执行部/领导层/管理员
   - 区域经理
3. 切换角色后，表格数据根据角色权限过滤（前端模拟）
4. 在页面顶部显示当前角色的数据范围说明

**代码位置**：
- 文件：`pages.js`
- 行号：535（角色徽章）

**注意事项**：
- ⚠️ 角色切换功能仅用于演示，实际项目中应从后台获取用户角色
- ⚠️ 如果其他页面也有角色徽章，需要为"监理人员动态"创建独立的角色切换函数

---

### 🟢 T3: 添加导出功能按钮

**任务描述**：
在筛选区域添加"导出"按钮，支持导出当前筛选结果。

**实现要点**：
1. 在"查询"和"重置"按钮右侧添加"导出"按钮
2. 按钮样式：绿色按钮，图标📥
3. 点击后弹出格式选择（Excel / Word）
4. 导出当前筛选条件下的人员数据

**代码位置**：
- 文件：`pages.js`
- 行号：546-549（筛选按钮区域）

**注意事项**：
- ⚠️ 可以复用其他页面的导出函数（如`doExport`），但需要确认不会影响其他页面

---

### 🟢 T4: 完善分页控件

**任务描述**：
将简单的分页信息扩展为完整的分页控件，包含上一页、页码、下一页按钮。

**实现要点**：
1. 保留"共X条"统计信息
2. 添加分页按钮：
   - 上一页按钮（当前为第1页时禁用）
   - 页码按钮（1、2、3...，当前页高亮）
   - 下一页按钮（当前为最后一页时禁用）
3. 点击页码按钮切换页面数据

**代码位置**：
- 文件：`pages.js`
- 行号：581（分页区域）

**注意事项**：
- ⚠️ 可以参考执行管理模块的分页实现
- ⚠️ 如果复用分页组件，确保不影响其他页面

---

### 🔴 T5: 实现二级页面 - 页面结构

**任务描述**：
创建二级页面（以人员为维度的派单列表），当用户点击一级页面的"查看派单"按钮时显示。

**实现要点**：
1. 创建二级页面的HTML结构
2. 页面标题：`监理人员动态 <small>(二级-人员派单列表 - 张三)</small>`
3. 添加"返回"按钮，点击返回一级页面
4. 页面布局：标题行 + 筛选区域 + 表格 + 分页

**代码位置**：
- 文件：`pages.js`
- 新增内容，在一级页面代码之后

**注意事项**：
- ⚠️ 二级页面应该是独立的HTML结构，不要修改一级页面
- ⚠️ 使用JavaScript控制一级页面和二级页面的显示/隐藏

---

### 🔴 T6: 实现二级页面 - 筛选条件

**任务描述**：
为二级页面添加筛选条件，允许用户筛选该人员参与的派单。

**实现要点**：
1. 筛选字段（9个）：
   - 监造形式：下拉选择（全部、驻厂、巡检）
   - 辅助人员：文本输入框
   - 制造厂：文本输入框
   - 总监代表：文本输入框
   - 区域经理：文本输入框
   - 计划开始时间：日期范围选择器
   - 计划结束时间：日期范围选择器
   - 发起时间：日期范围选择器
   - 派单状态：下拉选择（全部、未开始、进行中、已完成、超期）
2. 筛选按钮：查询、重置

**代码位置**：
- 文件：`pages.js`
- 二级页面的筛选区域

**注意事项**：
- ⚠️ 筛选条件布局可以分为两行，每行4-5个字段

---

### 🔴 T7: 实现二级页面 - 数据表格

**任务描述**：
为二级页面添加数据表格，展示该人员参与的所有派单信息。

**实现要点**：
1. 表格字段（12个）：
   - #、派单编号、监造形式、辅助人员、制造厂、总监代表
   - 计划开始时间、计划结束时间、发起时间、派单状态
   - 监造结束时间、剩余时间、工时、操作
2. 添加示例数据（3-5条派单记录）
3. 派单状态使用彩色状态灯显示
4. 监造形式使用标签（tag）显示

**代码位置**：
- 文件：`pages.js`
- 二级页面的表格区域

**注意事项**：
- ⚠️ 表格样式复用现有的`.data-table`类
- ⚠️ 确保表格字段与SRS文档一致

---

### 🔴 T8: 实现二级页面 - 操作按钮

**任务描述**：
为二级页面的每条派单记录添加"查看"按钮，点击后打开三级弹窗。

**实现要点**：
1. 在操作列添加"查看"按钮
2. 按钮样式：蓝色小按钮（`.btn-mini.btn-primary`）
3. 点击按钮调用函数`showPersonnelLevel3(personnelName, dispatchId)`
4. 函数功能：打开三级弹窗，显示该人员在该派单下的工时明细

**代码位置**：
- 文件：`pages.js`
- 二级页面表格的操作列

**注意事项**：
- ⚠️ 需要在`app.js`中创建`showPersonnelLevel3`函数

---

### 🟡 T9: 实现三级页面 - 弹窗结构

**任务描述**：
创建三级弹窗（辅助人员工时明细），当用户点击二级页面的"查看"按钮时显示。

**实现要点**：
1. 创建模态弹窗结构
2. 弹窗标题：`工时明细 - 张三 - PD-20260115-01`
3. 弹窗宽度：1200px
4. 弹窗内容：筛选区域 + 工时明细表格 + 工时日历
5. 关闭按钮：右上角×图标

**代码位置**：
- 文件：`pages.js`
- 新增弹窗HTML结构

**注意事项**：
- ⚠️ 弹窗样式复用现有的`.modal`类
- ⚠️ 弹窗显示/隐藏逻辑在`app.js`中实现

---

### 🟡 T10: 实现三级页面 - 工时明细表格

**任务描述**：
为三级弹窗添加工时明细表格，展示该人员在该派单下的每日工时记录。

**实现要点**：
1. 表格字段（6个）：
   - #、姓名、日期、上班时间、下班时间、签到状态、工时
2. 添加示例数据（10-15条工时记录）
3. 签到状态使用彩色标签：
   - 正常：绿色
   - 迟到：橙色
   - 早退：橙色
   - 缺勤：红色
4. 工时列显示天数（如"8小时"）

**代码位置**：
- 文件：`pages.js`
- 三级弹窗的表格区域

**注意事项**：
- ⚠️ 表格样式复用现有的`.data-table`类

---

### 🟡 T11: 实现三级页面 - 筛选条件

**任务描述**：
为三级弹窗添加筛选条件，允许用户筛选工时记录。

**实现要点**：
1. 筛选字段（2个）：
   - 日期范围：日期范围选择器（开始日期 ~ 结束日期）
   - 签到状态：下拉选择（全部、正常、迟到、早退、缺勤）
2. 筛选按钮：查询、重置

**代码位置**：
- 文件：`pages.js`
- 三级弹窗的筛选区域

**注意事项**：
- ⚠️ 筛选条件布局简洁，一行显示

---

### 🟡 T12: 实现三级页面 - 工时日历

**任务描述**：
为三级弹窗添加工时日历，可视化展示该人员的签到记录。

**实现要点**：
1. 日历布局：
   - 默认显示当前月份
   - 月份切换按钮（上一月、下一月）
   - 7列（周一~周日）× 5-6行（日期）
2. 签到标记：
   - 有签到记录的日期添加彩色标记（圆点或背景色）
   - 一天多个派单时，颜色加深
3. 交互功能：
   - 鼠标悬停：显示tooltip，展示当天所有派单编号
   - 点击日期：跳转到二级页面对应派单
4. 日历样式：
   - 当前日期高亮
   - 周末日期灰色显示
   - 非本月日期淡化显示

**代码位置**：
- 文件：`pages.js`
- 三级弹窗的日历区域
- 文件：`app.js`
- 日历渲染函数和交互逻辑

**注意事项**：
- ⚠️ 日历功能较复杂，可以使用简化版实现
- ⚠️ 日历样式需要在`styles.css`中添加独立的CSS类

---

## 实施顺序建议

**第一阶段（高优先级）**：
1. T1 → T5 → T6 → T7 → T8

**第二阶段（中优先级）**：
2. T2 → T9 → T10 → T11 → T12

**第三阶段（低优先级）**：
3. T3 → T4

---

## 验收标准

### 一级页面
- [ ] 筛选条件完整（12个字段）
- [ ] 角色切换功能正常
- [ ] 导出按钮可用
- [ ] 分页控件完整
- [ ] 点击"查看派单"按钮可进入二级页面

### 二级页面
- [ ] 页面标题显示人员姓名
- [ ] 返回按钮可返回一级页面
- [ ] 筛选条件完整（9个字段）
- [ ] 表格显示派单列表（12个字段）
- [ ] 点击"查看"按钮可打开三级弹窗

### 三级页面
- [ ] 弹窗标题显示人员姓名和派单编号
- [ ] 筛选条件完整（2个字段）
- [ ] 工时明细表格显示正常（6个字段）
- [ ] 工时日历显示当前月份
- [ ] 日历标记签到记录
- [ ] 鼠标悬停显示派单编号
- [ ] 点击日期可跳转

---

## 注意事项总结

1. ⚠️ **代码隔离**：所有新增代码必须独立，不影响其他菜单页面
2. ⚠️ **函数命名**：新增函数使用独特前缀（如`personnel_`），避免命名冲突
3. ⚠️ **样式复用**：优先复用现有CSS类，如需新增样式，使用独特的类名
4. ⚠️ **数据模拟**：所有数据为前端模拟，实际项目中应从后台API获取
5. ⚠️ **测试验证**：每完成一个任务，测试功能是否正常，确保不影响其他页面

---

**任务清单创建完成！**
**下一步**：按照任务顺序逐个实施
