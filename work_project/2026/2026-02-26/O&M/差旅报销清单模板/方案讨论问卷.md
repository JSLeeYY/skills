# 差旅报销台账系统 - 方案讨论问卷

请在每个问题后面标注你的选择（A/B/C/D），或者提出你的想法。

---

## 问题1：页面整体布局

你希望差旅报销台账页面采用什么布局？

**A. 上下分栏布局**
```
┌─────────────────────────────────┐
│ 顶部：筛选器 + 操作按钮          │
├─────────────────────────────────┤
│ 中部：统计卡片（总金额/单据数）  │
├─────────────────────────────────┤
│ 底部：数据表格（占主要空间）     │
└─────────────────────────────────┘
```

**B. 左右分栏布局**
```
┌──────────┬──────────────────────┐
│ 左侧：   │ 右侧：               │
│ - 筛选器 │ - 统计卡片           │
│ - 统计   │ - 数据表格           │
│          │                      │
└──────────┴──────────────────────┘
```

**C. Tab标签页布局**
```
┌─────────────────────────────────┐
│ [列表视图] [统计分析] [图表展示] │
├─────────────────────────────────┤
│ 根据选择的Tab显示不同内容        │
└─────────────────────────────────┘
```

**D. 其他（请描述）**

**你的选择：我要的是可以从不同维度查看全部数据的方式！**

---

## 问题2：数据表格显示哪些核心字段？

从74个字段中，表格主要显示哪些？（可多选，建议6-10个）

**A. 基础方案（8个字段）**
- 单据编号
- 员工编号
- 报销人
- 费用日期
- 费用项目
- 报销金额
- 发票号码
- 状态

**B. 详细方案（12个字段）**
- 单据编号
- 员工编号
- 报销人
- 部门
- 费用日期
- 费用项目
- 报销金额
- 发票金额
- 发票日期
- 合同编号
- 目的地
- 状态

**C. 自定义（请列出你想要的字段）**

**你的选择：__B，但是不够多，费用类型呢？不同的人根据不同费用类型，汇总不同的金额，____**

---

## 问题3：数据分类和筛选方式

你希望如何对数据进行分类查看？

**A. 按人员分组**
- 主筛选：选择人员
- 显示该人员的所有报销记录
- 统计该人员的总费用

**B. 按合同分组**
- 主筛选：选择合同
- 显示该合同下的所有费用
- 统计该合同的总费用

**C. 按费用类型分组**
- 主筛选：选择费用类型（差旅/招待/通讯等）
- 显示该类型的所有记录
- 统计该类型的总费用

**D. 多维度筛选（不分组）**
- 可同时按人员、合同、日期、类型筛选
- 显示符合条件的所有记录

**你的选择：____不够详细__**

---

## 问题4：员工编号生成规则

CSV中要新增"员工编号"列，规则是什么？

**A. 纯数字流水号**
- 格式：001, 002, 003...
- 简单直接

**B. EMP前缀+数字**
- 格式：EMP001, EMP002, EMP003...
- 易识别

**C. 部门代码+数字**
- 格式：FIN001（财务）, OPS001（运营）, PRD001（生产）...
- 包含部门信息

**D. 自定义规则（请描述）**

**你的选择：___上级单位制定的唯一编号，后期我会在系统user表中将这个字段对应好！___**

---

## 问题5：合同关联方式

报销单如何关联到合同？

**A. 导入时不关联**
- CSV导入后，合同字段为空
- 后期在页面上手动选择合同

**B. 根据项目名称自动匹配**
- 系统根据"项目"字段自动匹配合同
- 匹配不上的需要手动选择

**C. 导入前在CSV中添加合同编号列**
- 在Excel中先填好合同编号
- 导入时直接读取

**D. 暂不关联合同**
- 先不考虑合同维度

**你的选择：___B___**

---

## 问题6：统计分析需求

你需要哪些统计维度？（可多选）

**A. 按人员统计**
- 每个人的总费用
- 柱状图对比

**B. 按合同统计**
- 每个合同的总费用
- 饼图占比

**C. 按费用类型统计**
- 差旅费、招待费、通讯费等
- 饼图分布

**D. 按时间统计**
- 月度费用趋势
- 折线图

**E. 按部门统计**
- 各部门费用对比
- 柱状图

**F. 综合统计**
- 以上全部都要

**你的选择：___F，统计是单独的按钮，点击后会进入子页面进行展示___（可多选，如A+C+D）**

---

## 问题7：数据存储方式

数据存储在哪里？

**A. 浏览器LocalStorage**
- 优点：简单，无需后端
- 缺点：数据量限制（约5-10MB），清除浏览器数据会丢失

**B. JSON文件导入导出**
- 优点：灵活，可备份
- 缺点：每次使用需要导入

**C. 后端数据库（需要开发后端）**
- 优点：数据安全，支持多人使用
- 缺点：需要额外开发

**D. 混合方案（LocalStorage + 导出备份）**
- 平时用LocalStorage
- 定期导出JSON备份

**你的选择：_你不需要考虑数据存储啊！我肯定是持久化存储，目前只是画demo页面而已，考虑页面效果和业务逻辑而已_____**

---

## 问题8：CSV导入功能

导入CSV时需要哪些处理？

**A. 基础导入**
- 直接读取CSV
- 显示在表格中

**B. 智能导入**
- 自动识别员工（根据姓名匹配）
- 自动生成员工编号（新员工）
- 数据验证（必填项、格式检查）
- 显示导入结果（成功X条，失败X条）

**C. 增量导入**
- 检查重复单据（根据单据编号）
- 只导入新数据
- 更新已存在的数据

**你的选择：__导入肯定是一一对应的啊！你没理解吗？员工编号不是自己的，其次导入数据不止导入csv，还有excel！你再想想____**

---

## 问题9：单据详情展示

点击表格某一行时，如何显示详情？

**A. 右侧滑出抽屉**
- 表格不动，右侧弹出详情面板

**B. 弹窗模态框**
- 居中弹窗显示详情

**C. 跳转到详情页**
- 新页面显示完整信息

**D. 表格行展开**
- 点击行，下方展开详情

**你的选择：____A__**

---

## 问题10：功能优先级排序

请对以下功能按重要性排序（1最重要，8最不重要）

- [ ] CSV数据导入
- [ ] 数据表格展示（分页、排序）
- [ ] 多条件筛选（人员、日期、类型）
- [ ] 统计卡片（总金额、单据数）
- [ ] 图表分析（柱状图、饼图）
- [ ] 单据详情查看
- [ ] 数据导出Excel
- [ ] 新增/编辑报销单

**你的排序：**
1. ______
2. ______
3. ______
4. ______
5. ______
6. ______
7. ______
8. ______

---

## 问题11：其他需求

还有其他特殊需求或想法吗？请描述：

**你的补充：**
_____________________________________________
_____________________________________________
_____________________________________________

---

## 📝 填写完成后

请把你的答案告诉我，我会根据你的选择设计最终方案！
