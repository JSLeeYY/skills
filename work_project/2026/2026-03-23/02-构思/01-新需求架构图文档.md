# 新需求架构图文档

## 1. 系统上下文图

```mermaid
flowchart LR
    subgraph 业务参与方
        A1[监造管理部门]
        A2[监造工程师]
        A3[项目负责人/采购员]
        A4[供应商/制造厂]
        A5[审查人/总监]
        A6[培训学员]
    end

    subgraph 新监造平台
        P[供方监造管理平台]
    end

    subgraph 外围能力
        E1[统一认证平台]
        E2[视频采集与监控平台]
        E3[OCR/防造假识别服务]
        E4[消息通知服务]
        E5[法规标准与知识资源库]
    end

    A1 --> P
    A2 --> P
    A3 --> P
    A4 --> P
    A5 --> P
    A6 --> P

    P --> E1
    P --> E2
    P --> E3
    P --> E4
    P --> E5
```

## 2. 逻辑架构图

下图改为静态 SVG 分层图，保留原有全部节点，避免 Mermaid 自动布局导致文字过小、连线拥挤。

## ![逻辑架构图](./01-%E6%96%B0%E9%9C%80%E6%B1%82%E9%80%BB%E8%BE%91%E6%9E%B6%E6%9E%84%E5%9B%BE.svg)3. 部署架构图

```mermaid
flowchart TB
    subgraph 终端侧
        U1[监造人员客户端<br/>麒麟V10]
        U2[供应商浏览器]
        U3[移动端 App/H5]
        U4[培训与管理端]
        U5[现场摄像头/执法记录仪]
    end

    subgraph 网络安全区
        N1[VPN接入]
        N2[内网访问控制]
        N3[HTTPS反向代理]
    end

    subgraph 应用服务区
        A1[业务应用服务<br/>Spring Boot 多模块单体]
        A2[工作流服务<br/>Flowable]
        A3[文件服务]
        A4[消息通知服务]
        A5[统计报表服务]
        A6[智能识别集成服务]
    end

    subgraph 中间件区
        M1[Redis]
        M2[RabbitMQ]
    end

    subgraph 数据区
        DB1[达梦V8 业务库]
        DB2[达梦V8 流程库或独立Schema]
        FS1[附件存储]
        FS2[视频存储]
        KB1[资源知识库]
    end

    subgraph 外部能力
        X1[统一认证平台]
        X2[OCR/图像识别服务]
        X3[二维码解析与防造假服务]
        X4[短信/消息推送]
        X5[第三方视频平台]
    end

    U1 --> N1
    U2 --> N1
    U3 --> N1
    U4 --> N1
    U5 --> X5

    N1 --> N2
    N2 --> N3
    N3 --> A1

    A1 --> A2
    A1 --> A3
    A1 --> A4
    A1 --> A5
    A1 --> A6

    A1 --> M1
    A1 --> M2
    A2 --> DB2
    A1 --> DB1
    A3 --> FS1
    A6 --> FS2
    A6 --> KB1

    A1 --> X1
    A6 --> X2
    A6 --> X3
    A4 --> X4
    A6 --> X5
```

## 4. 核心数据关系图

```mermaid
flowchart LR
    P1[项目]
    P2[采购包/监造任务]
    P3[合同]
    P4[物资/物项]
    P5[供方资料]
    P6[监造策划]
    P7[质量计划]
    P8[W R H见证点]
    P9[监督记录/巡检/抽验]
    P10[质量问题单据]
    P11[整改与复核]
    P12[验收与放行]
    P13[完工总结]
    P14[追溯链]

    P1 --> P2
    P2 --> P3
    P3 --> P4
    P2 --> P5
    P2 --> P6
    P6 --> P7
    P7 --> P8
    P8 --> P9
    P9 --> P10
    P10 --> P11
    P11 --> P12
    P12 --> P13
    P4 --> P14
    P5 --> P14
    P7 --> P14
    P9 --> P14
    P10 --> P14
    P12 --> P14
    P13 --> P14
```

## 5. 架构落地结论

- 参考项目可直接复用的架构思想：
  - 单体多模块后端组织方式。
  - 流程审批由`Flowable`承载。
  - 权限与数据权限由`Shiro`承载。
  - 附件上传、下载、预览已经有统一文件服务。
  - 缓存/待办/消息联动可沿用`Redis + RabbitMQ`模式。
- 新系统与参考项目的主要差异：
  - 数据库需从参考项目当前的`MySQL`落到新需求指定的`达梦V8`。
  - 功能域从现有“项目派工、交底、日志、周报、问题、交接”扩展到“供方文件审核、质量计划、验收放行、OCR防造假、培训考试、付款台账”等全链条。
  - 现场视频、OCR、防造假、二维码、知识资源库均属于新增集成能力。
- 建议的技术边界：
  - 主业务库与流程库分开，至少分`Schema`，便于迁移、归档和性能治理。
  - 供应商协同、审批流、问题闭环、文件归档必须共享统一编码体系，保证追溯链可闭环。
  - 智能识别能力应通过集成服务接入，不要直接耦合到核心业务模块。
