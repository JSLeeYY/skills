# 服务器磁盘技术实施总方案（单文档交付版）

## 1. 文档说明

本文档为**单文档交付版**，将以下内容全部合并在一个 Markdown 文件中，便于直接交付、打印、评审、执行：

1. 生产可用技术方案
2. 目录规范
3. 分系统实施方案
4. 现场实施 SOP
5. 割接检查清单
6. `/etc/fstab` 配置示例
7. 全部操作脚本源码
8. 风险预案
9. 回滚方案
10. 注意事项

> 适用现场：
> - 三方、安全平台：`192.168.0.111`
> - 门户、CAS单点登录、泛微：`192.168.0.3`
> - 对外平台：`192.168.0.4`

---

## 2. 现场环境与结论

### 2.1 服务器与业务系统映射

| 业务系统 | 服务器IP | 操作系统 | 当前系统盘使用率 | 备注 |
|---|---|---|---:|---|
| 三方、安全平台 | 192.168.0.111 | CentOS 7.9 | 88% | MySQL 5.7.36，非 systemd |
| 门户、CAS单点登录、泛微 | 192.168.0.3 | Ubuntu 18.04.6 | 89% | 双 MySQL 实例，非 systemd |
| 对外平台 | 192.168.0.4 | CentOS 7.9 | 93% | 当前 `/dev/sdb` 已挂载 `/filedata` |

### 2.2 共享盘现状

- 共享云盘设备：`/dev/sdb`
- 文件系统现状：整盘 ext4
- 历史 UUID：`7baac44d-db8c-4d12-b152-61dd02930f1b`
- 容量：2T

### 2.3 MySQL 现场参数

#### 三方、安全平台（192.168.0.111）
- 版本：`MySQL 5.7.36`
- datadir：`/data/mysql57/data`
- 端口：`3603`
- 启动方式：`/usr/local/mysql/bin/mysqld_safe --datadir=/data/mysql57/data ...`
- 非 systemd 管理

#### 门户、CAS单点登录、泛微（192.168.0.3）
- 实例1（3308）
  - datadir：`/opt/mysql/mydata/3308/data`
  - error.log：`/opt/mysql/mylog/3308/error.log`
  - 启动方式：`/opt/mysql/mysql/base/bin/mysqld_safe --defaults-file=/opt/mysql/my.conf --user=root`
- 实例2（3306）
  - datadir：`/usr/local/weaver_mysql/data`
  - error.log：`/usr/local/weaver_mysql/data/mysqld.log`
  - 启动方式：`/usr/local/weaver_mysql/base/bin/mysqld_safe --datadir=/usr/local/weaver_mysql/data ...`
- 两个实例全部纳入迁移

#### 对外平台（192.168.0.4）
- datadir：`/data/mysql57/data`
- 端口：`3603`
- 启动方式：`/usr/local/mysql/bin/mysqld_safe --datadir=/data/mysql57/data ...`
- 非 systemd 管理

---

## 3. 总体技术方案

## 3.1 关键生产原则

**禁止**三台服务器同时以读写方式直挂同一个 ext4 文件系统。  
原因：ext4 不是集群文件系统，多主机同时读写同一 ext4 块设备存在元数据损坏风险，不属于生产可用方案。

因此，本方案采用：

### 共享盘三分区独占模型
1. 在 `/dev/sdb` 上重建 3 个分区；
2. 每台服务器只挂自己的分区；
3. 每个节点本地统一挂载到 `/filedata/nodeX`；
4. 通过 **bind mount 为主、软链接为辅**，让业务继续使用原始路径。

## 3.2 分区规划

| 分区 | 挂载对象 | 建议容量 | 标签 | 挂载点 |
|---|---|---:|---|---|
| `/dev/sdb1` | 192.168.0.111 | 500G | `FILEDATA_NODE1` | `/filedata/node1` |
| `/dev/sdb2` | 192.168.0.3 | 800G | `FILEDATA_NODE2` | `/filedata/node2` |
| `/dev/sdb3` | 192.168.0.4 | 其余全部 | `FILEDATA_NODE3` | `/filedata/node3` |

> 若现场确认 Node1/Node3 数据量更大，可调整为 `600G / 800G / 其余`。

## 3.3 目录规范

```text
/filedata/nodeX/
├─ mysql/
│  ├─ mysql57_3603/
│  ├─ mysql3308/
│  └─ weaver3306/
├─ logs/
│  ├─ services/
│  └─ system/
└─ meta/
   ├─ manifests/
   │  ├─ discovered_logs.tsv
   │  ├─ approved_logs.tsv
   │  └─ migration_state.tsv
   ├─ rollback/
   └─ logs/
```

## 3.4 技术路线

- **MySQL 数据目录**：使用 `bind mount`
- **宿主机业务日志目录**：优先 `bind mount`
- **离散单文件日志**：使用软链接
- **系统日志**：先发现 -> 输出说明清单 -> 人工确认 -> 再迁移
- **Docker 容器日志**：本次明确不迁移
- **切换方式**：单台、单服务、短暂停服、可回滚

---

## 4. 按业务系统的实施落点

## 4.1 三方、安全平台（192.168.0.111）
- MySQL 原路径：`/data/mysql57/data`
- MySQL 目标目录：`/filedata/node1/mysql/mysql57_3603`
- 业务日志目标目录：`/filedata/node1/logs/services/...`
- 系统日志目标目录：`/filedata/node1/logs/system/...`

## 4.2 门户、CAS单点登录、泛微（192.168.0.3）
- 实例 3308 原路径：`/opt/mysql/mydata/3308/data`
- 实例 3308 目标目录：`/filedata/node2/mysql/mysql3308`
- 实例 3306 原路径：`/usr/local/weaver_mysql/data`
- 实例 3306 目标目录：`/filedata/node2/mysql/weaver3306`
- 业务日志目标目录：`/filedata/node2/logs/services/...`
- 系统日志目标目录：`/filedata/node2/logs/system/...`
- **明确排除 Docker 日志**

## 4.3 对外平台（192.168.0.4）
- MySQL 原路径：`/data/mysql57/data`
- MySQL 目标目录：`/filedata/node3/mysql/mysql57_3603`
- 业务日志目标目录：`/filedata/node3/logs/services/...`
- 系统日志目标目录：`/filedata/node3/logs/system/...`

---

## 5. MySQL 迁移方案

## 5.1 统一原则
- 不修改业务配置中的 datadir 路径
- 切换后仍保持原路径不变
- 共享盘目录通过 `mount --bind` 回挂到原路径
- 切换前必须停库
- 切换后必须校验进程、端口、挂载
- 本地原目录保留 `.bak_时间戳`，用于回滚

## 5.2 通用步骤
```text
1. 停止 MySQL 实例
2. rsync 原 datadir -> 共享盘目标目录
3. mv 原 datadir -> datadir.bak_时间戳
4. mkdir 原 datadir
5. mount --bind 共享盘目录 -> 原 datadir
6. 追加 /etc/fstab
7. 启动实例
8. 校验进程/端口/bind mount
```

## 5.3 实例映射表

| 业务系统 | 原 datadir | 共享盘目录 | 挂回原路径 |
|---|---|---|---|
| 三方、安全平台 | `/data/mysql57/data` | `/filedata/node1/mysql/mysql57_3603` | `/data/mysql57/data` |
| 门户/CAS/泛微 3308 | `/opt/mysql/mydata/3308/data` | `/filedata/node2/mysql/mysql3308` | `/opt/mysql/mydata/3308/data` |
| 门户/CAS/泛微 3306 | `/usr/local/weaver_mysql/data` | `/filedata/node2/mysql/weaver3306` | `/usr/local/weaver_mysql/data` |
| 对外平台 | `/data/mysql57/data` | `/filedata/node3/mysql/mysql57_3603` | `/data/mysql57/data` |

---

## 6. 日志迁移方案

## 6.1 发现与审核机制
日志不允许盲迁，必须先生成 `discovered_logs.tsv`，再人工审核为 `approved_logs.tsv`。

TSV 字段：
- `approved`
- `type`
- `path`
- `service`
- `description`
- `target_subdir`
- `method`

## 6.2 系统日志默认候选说明

| 路径 | 说明 | 建议 |
|---|---|---|
| `/var/log` | 系统日志根目录 | 优先整体迁移 |
| `/var/log/messages` | CentOS 综合系统日志 | 若不整迁 `/var/log`，可单独迁 |
| `/var/log/secure` | 安全认证日志 | 建议迁 |
| `/var/log/cron` | 定时任务日志 | 建议迁 |
| `/var/log/audit` | 审计日志 | 建议迁 |
| `/var/log/syslog` | Ubuntu 综合系统日志 | 建议迁 |
| `/var/log/auth.log` | Ubuntu 认证安全日志 | 建议迁 |
| `/var/log/kern.log` | Ubuntu 内核日志 | 按需迁 |

## 6.3 迁移规则
- 目录日志：优先 `bind mount`
- 单文件日志：使用软链接
- 不允许父路径与子路径同时批准
- Node2 不允许包含 Docker 路径
- 危险根路径禁止迁移：`/`、`/var`、`/opt`、`/usr`、`/data`、`/usr/local` 等

---

## 7. 风险预案

### 风险1：三机同挂一个 ext4
- 级别：高
- 预案：必须改为三分区独占模型

### 风险2：Node3 当前整盘挂载 `/filedata`
- 级别：高
- 预案：重分区前必须确认旧 `/filedata` 无正式生产数据

### 风险3：Node2 双实例切换失败
- 级别：高
- 预案：3308、3306 串行迁移，前一实例不过不继续下一实例

### 风险4：日志误迁或父子路径冲突
- 级别：中
- 预案：必须人工审核 `approved_logs.tsv`；脚本做父子冲突校验

### 风险5：权限异常导致服务无法写入
- 级别：中
- 预案：脚本继承原目录属主属组并重新 `chown`

### 风险6：释放空间不及预期
- 级别：中
- 说明：本次不迁移 `/opt` 程序安装目录，仅迁 MySQL 和日志
- 预案：验收按“数据库与日志已落共享盘”口径，不误承诺安装目录瘦身

---

## 8. 回滚方案

## 8.1 触发条件
任一条件满足即可回滚：
- MySQL 无法启动
- 页面不可用
- 登录失败
- 数据读写异常
- 日志停止生成
- bind mount 丢失
- 权限异常

## 8.2 回滚原则
- 按 `migration_state.tsv` 逆序回滚
- 先卸载 bind mount / 删除软链接
- 再恢复 `.bak_时间戳`
- 再清理 `/etc/fstab` 本次追加项

## 8.3 回滚分类
新版回滚脚本支持：
- 全量回滚：`all`
- 仅回滚 MySQL：`mysql`
- 仅回滚日志：`log`

---

## 9. `/etc/fstab` 示例

### 9.1 Node1
```fstab
UUID=<NODE1_PART_UUID>  /filedata/node1  ext4  defaults,noatime,nodiratime  0 2
/filedata/node1/mysql/mysql57_3603  /data/mysql57/data  none  bind  0 0
```

### 9.2 Node2
```fstab
UUID=<NODE2_PART_UUID>  /filedata/node2  ext4  defaults,noatime,nodiratime  0 2
/filedata/node2/mysql/mysql3308   /opt/mysql/mydata/3308/data   none  bind  0 0
/filedata/node2/mysql/weaver3306  /usr/local/weaver_mysql/data  none  bind  0 0
```

### 9.3 Node3
```fstab
UUID=<NODE3_PART_UUID>  /filedata/node3  ext4  defaults,noatime,nodiratime  0 2
/filedata/node3/mysql/mysql57_3603  /data/mysql57/data  none  bind  0 0
```

---

## 10. 现场实施 SOP

# 服务器磁盘迁移现场实施SOP

## 1. 目标

本SOP仅服务于现场实施，不讲汇报口径，只讲执行顺序、停点、检查点、回退点。

适用范围：
- 三方、安全平台：`192.168.0.111`
- 门户、CAS单点登录、泛微：`192.168.0.3`
- 对外平台：`192.168.0.4`

适用内容：
- 共享盘分区与挂载
- MySQL 数据目录迁移
- 宿主机业务日志迁移
- 系统日志迁移
- 回滚处理

不适用内容：
- Docker 容器日志迁移
- `/opt` 程序安装目录迁移
- 业务代码改造
- 应用配置重构

---

## 2. 实施总原则

1. 一次只动一台服务器。
2. 一次只切一个核心服务。
3. 先做共享盘可用性，再做 MySQL，再做日志。
4. 日志必须“先发现、再审核、后迁移”。
5. 每完成一步，立即做验证；验证不通过，立即回滚，不进入下一步。
6. 本地 `.bak_时间戳` 目录在验收通过前禁止删除。

---

## 3. 现场角色分工建议

### 3.1 执行人
负责实际执行脚本与命令。

### 3.2 业务确认人
负责确认：
- MySQL 是否可连
- 页面是否可访问
- 关键业务是否可用
- 日志是否持续写入

### 3.3 记录人
负责记录：
- 开始时间
- 停止时间
- 恢复时间
- 验证结果
- 回滚点
- 异常现象

---

## 4. 总体执行顺序

### 阶段A：共享盘基础准备
1. 在对外平台 `192.168.0.4` 上执行共享盘重分区与 node3 初始化。
2. 在三方、安全平台 `192.168.0.111` 上挂载 node1 分区并初始化目录。
3. 在门户/CAS/泛微 `192.168.0.3` 上挂载 node2 分区并初始化目录。

### 阶段B：MySQL 迁移
1. 三方、安全平台 MySQL
2. 门户/CAS/泛微 MySQL 3308
3. 门户/CAS/泛微 MySQL 3306
4. 对外平台 MySQL

### 阶段C：日志迁移
1. 各节点运行日志发现脚本
2. 审核日志候选清单
3. 分批执行日志迁移
4. 每批迁移后立即验证

---

## 5. 阶段A：共享盘基础准备SOP

## 5.1 在 192.168.0.4 执行共享盘重分区

### 步骤1：确认旧 `/filedata` 无正式生产数据
执行前确认：
- `/filedata` 只存在少量测试/空目录
- 无业务服务正在直接读写旧 `/filedata`

建议命令：
```bash
mount | grep filedata
find /filedata -maxdepth 2 -type f | head
lsof +D /filedata | head
```

### 步骤2：执行 node3 共享盘初始化脚本
脚本：
- `node3_01_init_shared_disk.sh`

执行：
```bash
bash node3_01_init_shared_disk.sh
```

### 步骤3：确认结果
检查项：
```bash
lsblk
blkid /dev/sdb1 /dev/sdb2 /dev/sdb3
df -h | grep filedata
find /filedata/node3 -maxdepth 3 -type d | sort
cat /tmp/node1.fstab.sample
cat /tmp/node2.fstab.sample
cat /tmp/node3.fstab.sample
```

通过标准：
- `/dev/sdb1 /dev/sdb2 /dev/sdb3` 均存在
- `FILEDATA_NODE1/2/3` label 正确
- `/filedata/node3` 已成功挂载
- `mysql/logs/meta` 目录已创建

回退点：
- 若此阶段失败，停止后续所有动作，不进入 MySQL 迁移阶段。

---

## 5.2 在 192.168.0.111 挂载 node1 分区

脚本：
- `node1_01_init_shared_disk.sh`

执行：
```bash
bash node1_01_init_shared_disk.sh
```

检查：
```bash
df -h | grep /filedata/node1
find /filedata/node1 -maxdepth 3 -type d | sort
cat /tmp/node1.fstab.sample
```

通过标准：
- `/filedata/node1` 正常挂载
- `mysql/logs/meta` 目录齐全

---

## 5.3 在 192.168.0.3 挂载 node2 分区

脚本：
- `node2_01_init_shared_disk.sh`

执行：
```bash
bash node2_01_init_shared_disk.sh
```

检查：
```bash
df -h | grep /filedata/node2
find /filedata/node2 -maxdepth 3 -type d | sort
cat /tmp/node2.fstab.sample
```

通过标准：
- `/filedata/node2` 正常挂载
- `mysql/logs/meta` 目录齐全

---

## 6. 阶段B：MySQL迁移SOP

## 6.1 三方、安全平台（192.168.0.111）

### 原路径
- datadir：`/data/mysql57/data`
- 目标：`/filedata/node1/mysql/mysql57_3603`

### 执行步骤
1. 通知业务进入短暂停写窗口。
2. 执行脚本：`node1_02_migrate_mysql.sh`
3. 验证进程与端口。
4. 验证业务连接。

执行：
```bash
bash node1_02_migrate_mysql.sh
bash node1_02_migrate_mysql.sh --dry-run
```

验证：
```bash
ps -ef | grep mysqld
ss -lntp | grep 3603
mount | grep "/data/mysql57/data"
df -h /data/mysql57/data
ls -ld /data/mysql57/data /filedata/node1/mysql/mysql57_3603
```

业务验证：
- 能连接数据库
- 业务读操作正常
- 业务写操作正常

失败回退：
- `node1_99_rollback_all.sh`
- `bash node1_99_rollback_all.sh mysql`

---

## 6.2 门户、CAS单点登录、泛微（192.168.0.3）

### 先迁 3308，后迁 3306
绝对禁止两套实例同时切换。

### 实例1：3308
- datadir：`/opt/mysql/mydata/3308/data`
- 目标：`/filedata/node2/mysql/mysql3308`

### 实例2：3306
- datadir：`/usr/local/weaver_mysql/data`
- 目标：`/filedata/node2/mysql/weaver3306`

### 执行步骤
1. 先通知 3308 关联业务停写。
2. 执行：`node2_02_migrate_mysql.sh`
3. 先核验 3308，再核验 3306。
4. 任一实例异常，不继续后续日志迁移。

验证命令：
```bash
ps -ef | grep mysqld
ss -lntp | egrep '3306|3308'
mount | egrep '/opt/mysql/mydata/3308/data|/usr/local/weaver_mysql/data'
df -h /opt/mysql/mydata/3308/data
df -h /usr/local/weaver_mysql/data
```

业务验证：
- 门户登录正常
- CAS 跳转正常
- 泛微页面正常
- 数据写入正常

失败回退：
- `node2_99_rollback_all.sh`
- `bash node2_99_rollback_all.sh mysql`

---

## 6.3 对外平台（192.168.0.4）

### 原路径
- datadir：`/data/mysql57/data`
- 目标：`/filedata/node3/mysql/mysql57_3603`

### 执行步骤
1. 通知业务进入短暂停写窗口。
2. 执行脚本：`node3_02_migrate_mysql.sh`
3. 验证进程、端口、挂载、读写。

执行：
```bash
bash node3_02_migrate_mysql.sh
bash node3_02_migrate_mysql.sh --dry-run
```

验证：
```bash
ps -ef | grep mysqld
ss -lntp | grep 3603
mount | grep "/data/mysql57/data"
df -h /data/mysql57/data
```

失败回退：
- `node3_99_rollback_all.sh`
- `bash node3_99_rollback_all.sh mysql`

---

## 7. 阶段C：日志迁移SOP

## 7.1 先发现，不直接迁移

执行脚本：
- `node1_03_discover_logs.sh`
- `node2_03_discover_logs.sh`
- `node3_03_discover_logs.sh`

执行：
```bash
bash nodeX_03_discover_logs.sh
```

生成文件：
- `/filedata/nodeX/meta/manifests/discovered_logs.tsv`

### 审核规则
审核时重点看：
1. `path` 是否真的是日志目录/日志文件
2. `description` 是否符合实际用途
3. `service` 是否归属正确
4. `target_subdir` 是否合理
5. `method` 是否合理
6. 是否与父路径/子路径重复
7. node2 是否误包含 Docker 路径

### 审核输出
把 `discovered_logs.tsv` 复制一份为：
- `approved_logs.tsv`

只把确认要迁移的行改成：
- `approved=Y`

其余保留 `N`

---

## 7.2 执行日志迁移

执行脚本：
- `node1_04_apply_logs_migration.sh`
- `node2_04_apply_logs_migration.sh`
- `node3_04_apply_logs_migration.sh`

执行：
```bash
bash nodeX_04_apply_logs_migration.sh
bash nodeX_04_apply_logs_migration.sh --dry-run
```

### 执行后检查
```bash
mount | grep /var/log
find /filedata/nodeX/logs -maxdepth 3 -type d | sort
cat /filedata/nodeX/meta/manifests/migration_state.tsv
```

### 业务验证
- 服务仍在写日志
- 新日志文件生成在共享盘对应目录
- 原路径仍可访问
- 监控/采集程序未报错

---

## 8. 回滚 SOP

## 8.1 回滚触发条件
满足任一即回滚：
- MySQL 启动失败
- 页面不可用
- 登录失败
- 数据写入异常
- 日志不再产生
- 挂载关系错误
- 权限异常

## 8.2 回滚脚本
- `node1_99_rollback_all.sh`
- `node2_99_rollback_all.sh`
- `node3_99_rollback_all.sh`

执行：
```bash
bash nodeX_99_rollback_all.sh
bash nodeX_99_rollback_all.sh mysql
bash nodeX_99_rollback_all.sh log
```

回滚后验证：
```bash
mount | grep -E '/data/mysql57/data|/opt/mysql/mydata/3308/data|/usr/local/weaver_mysql/data|/var/log'
ps -ef | grep mysqld
ss -lntp | egrep '3306|3308|3603'
```

---

## 9. 现场禁止事项

1. 禁止三台服务器同时直挂同一个 ext4 文件系统读写。
2. 禁止在未审核 `approved_logs.tsv` 的情况下直接执行日志迁移。
3. 禁止 Node2 在日志迁移中纳入 Docker 日志。
4. 禁止在 MySQL 未验证成功前删除本地 `.bak_` 目录。
5. 禁止双 MySQL 实例同时切换。
6. 禁止一边切换一边修改业务配置。

---

## 10. 最小现场执行顺序总结

### 第一步
在 `192.168.0.4`：
```bash
bash node3_01_init_shared_disk.sh
```

### 第二步
在 `192.168.0.111`：
```bash
bash node1_01_init_shared_disk.sh
```

### 第三步
在 `192.168.0.3`：
```bash
bash node2_01_init_shared_disk.sh
```

### 第四步
依次迁移 MySQL：
```bash
bash node1_02_migrate_mysql.sh
bash node2_02_migrate_mysql.sh
bash node3_02_migrate_mysql.sh
```

### 第五步
各节点先发现日志：
```bash
bash nodeX_03_discover_logs.sh
```

### 第六步
人工审核 `approved_logs.tsv`

### 第七步
执行日志迁移：
```bash
bash nodeX_04_apply_logs_migration.sh
```

### 第八步
有异常立即执行：
```bash
bash nodeX_99_rollback_all.sh
```

---

## 11. 割接检查清单

# 服务器磁盘迁移割接检查清单

> 用法：本清单只做现场勾检。每完成一项打勾；任一关键项不通过，立即停止并回滚，不进入下一步。

---

## 1. 实施前总检查

### 1.1 基础信息确认
- [ ] 已确认三方、安全平台对应 `192.168.0.111`
- [ ] 已确认门户、CAS单点登录、泛微对应 `192.168.0.3`
- [ ] 已确认对外平台对应 `192.168.0.4`
- [ ] 已确认本次不迁移 Docker 容器日志
- [ ] 已确认本次不迁移 `/opt` 程序安装目录
- [ ] 已确认实施窗口已获现场授权
- [ ] 已确认可接受单台、单服务短暂停服切换

### 1.2 共享盘检查
- [ ] 已确认共享盘设备为 `/dev/sdb`
- [ ] 已确认旧 `/filedata` 中无需要保留的生产数据
- [ ] 已确认 Node3 当前旧挂载可安全卸载
- [ ] 已确认不会继续沿用“三机同挂一个 ext4”方案

### 1.3 脚本准备检查
- [ ] 已拿到 `node1_01/02/03/04/99` 脚本
- [ ] 已拿到 `node2_01/02/03/04/99` 脚本
- [ ] 已拿到 `node3_01/02/03/04/99` 脚本
- [ ] 已拿到 `node1.fstab.sample`
- [ ] 已拿到 `node2.fstab.sample`
- [ ] 已拿到 `node3.fstab.sample`

---

## 2. Node3 共享盘重分区检查清单

### 2.1 执行前
- [ ] `mount | grep /filedata` 已检查
- [ ] `lsof +D /filedata` 已检查
- [ ] 已确认旧 `/filedata` 没有正式生产文件在使用

### 2.2 执行后
- [ ] `/dev/sdb1` 已创建
- [ ] `/dev/sdb2` 已创建
- [ ] `/dev/sdb3` 已创建
- [ ] `FILEDATA_NODE1` label 正确
- [ ] `FILEDATA_NODE2` label 正确
- [ ] `FILEDATA_NODE3` label 正确
- [ ] `/filedata/node3` 已挂载成功
- [ ] `/filedata/node3/mysql` 已创建
- [ ] `/filedata/node3/logs` 已创建
- [ ] `/filedata/node3/meta` 已创建
- [ ] `/tmp/node1.fstab.sample` 已生成
- [ ] `/tmp/node2.fstab.sample` 已生成
- [ ] `/tmp/node3.fstab.sample` 已生成

### 2.3 失败即停条件
- [ ] 任一分区未创建成功
- [ ] 任一分区无法格式化
- [ ] `/filedata/node3` 无法挂载
- [ ] 目录结构不完整

> 若上面任一项失败：停止后续全部动作。

---

## 3. Node1 挂载与初始化检查清单

### 3.1 执行前
- [ ] `blkid /dev/sdb1` 能识别分区
- [ ] `/filedata/node1` 已创建

### 3.2 执行后
- [ ] `/filedata/node1` 已挂载成功
- [ ] `/filedata/node1/mysql/mysql57_3603` 已创建
- [ ] `/filedata/node1/logs/services` 已创建
- [ ] `/filedata/node1/logs/system` 已创建
- [ ] `/filedata/node1/meta/manifests` 已创建
- [ ] `/tmp/node1.fstab.sample` 已生成

---

## 4. Node2 挂载与初始化检查清单

### 4.1 执行前
- [ ] `blkid /dev/sdb2` 能识别分区
- [ ] `/filedata/node2` 已创建

### 4.2 执行后
- [ ] `/filedata/node2` 已挂载成功
- [ ] `/filedata/node2/mysql/mysql3308` 已创建
- [ ] `/filedata/node2/mysql/weaver3306` 已创建
- [ ] `/filedata/node2/logs/services` 已创建
- [ ] `/filedata/node2/logs/system` 已创建
- [ ] `/filedata/node2/meta/manifests` 已创建
- [ ] `/tmp/node2.fstab.sample` 已生成

---

## 5. MySQL 割接检查清单

## 5.1 三方、安全平台（192.168.0.111）

### 割接前
- [ ] 已确认当前 datadir 为 `/data/mysql57/data`
- [ ] 已确认业务允许短暂停写
- [ ] 已确认共享盘目标目录存在：`/filedata/node1/mysql/mysql57_3603`

### 割接后
- [ ] mysqld 进程已恢复
- [ ] `3603` 端口已监听
- [ ] `/data/mysql57/data` 已为 bind mount
- [ ] `df -h /data/mysql57/data` 显示已落到共享盘
- [ ] 原目录 `.bak_时间戳` 已保留
- [ ] 数据库连接正常
- [ ] 业务读写正常

### 不通过即回滚
- [ ] 进程未恢复
- [ ] 端口未监听
- [ ] 业务报错
- [ ] 挂载关系不对

---

## 5.2 门户、CAS单点登录、泛微（192.168.0.3）

### 3308 割接前
- [ ] 已确认 datadir 为 `/opt/mysql/mydata/3308/data`
- [ ] 已确认共享盘目标目录存在：`/filedata/node2/mysql/mysql3308`
- [ ] 已确认只切 3308，不同时切 3306

### 3308 割接后
- [ ] 3308 实例已恢复
- [ ] `3308` 端口已监听
- [ ] 原路径已 bind mount
- [ ] `.bak_时间戳` 已保留
- [ ] 3308 关联业务验证正常

### 3306 割接前
- [ ] 已确认 3308 已验证通过
- [ ] 已确认 datadir 为 `/usr/local/weaver_mysql/data`
- [ ] 已确认共享盘目标目录存在：`/filedata/node2/mysql/weaver3306`

### 3306 割接后
- [ ] 3306 实例已恢复
- [ ] `3306` 端口已监听
- [ ] 原路径已 bind mount
- [ ] `.bak_时间戳` 已保留
- [ ] 门户登录正常
- [ ] CAS 跳转正常
- [ ] 泛微访问正常

### 不通过即回滚
- [ ] 任一实例起不来
- [ ] 任一实例端口不通
- [ ] 门户/CAS/泛微异常

---

## 5.3 对外平台（192.168.0.4）

### 割接前
- [ ] 已确认当前 datadir 为 `/data/mysql57/data`
- [ ] 已确认共享盘目标目录存在：`/filedata/node3/mysql/mysql57_3603`
- [ ] 已确认业务允许短暂停写

### 割接后
- [ ] mysqld 进程已恢复
- [ ] `3603` 端口已监听
- [ ] `/data/mysql57/data` 已为 bind mount
- [ ] `.bak_时间戳` 已保留
- [ ] 外部平台业务验证正常

---

## 6. 日志发现清单审核检查

### 6.1 发现脚本执行
- [ ] node1 已生成 `discovered_logs.tsv`
- [ ] node2 已生成 `discovered_logs.tsv`
- [ ] node3 已生成 `discovered_logs.tsv`

### 6.2 审核项
- [ ] 每条候选路径都已确认是否真为日志路径
- [ ] 每条候选路径都已补齐/确认用途说明
- [ ] 每条候选路径都已确认归属服务
- [ ] 每条候选路径都已确认目标子目录
- [ ] 已确认 `method` 合理（目录=bind，文件=symlink）
- [ ] 已确认没有父子路径重复批准
- [ ] 已确认 node2 不包含 Docker 日志路径
- [ ] 已生成 `approved_logs.tsv`
- [ ] 只有明确批准项才改为 `Y`

---

## 7. 日志迁移执行检查

### 7.1 执行前
- [ ] `approved_logs.tsv` 已人工审核完成
- [ ] 已确认当前批次迁移路径不会互相冲突
- [ ] 已确认关键业务有人在线验证

### 7.2 执行后
- [ ] 迁移状态已记录到 `migration_state.tsv`
- [ ] 目录类日志已成功 bind mount
- [ ] 文件类日志已成功建立软链接
- [ ] 原始 `.bak_时间戳` 已保留
- [ ] 新日志持续写入共享盘目录
- [ ] 原路径访问正常
- [ ] 监控/采集程序未异常

### 7.3 不通过即回滚
- [ ] 日志停止生成
- [ ] 权限异常
- [ ] 原路径失效
- [ ] 服务异常

---

## 8. 回滚检查清单

### 8.1 触发前
- [ ] 已确认问题不能通过现场微调快速恢复
- [ ] 已通知业务执行回滚
- [ ] 已定位对应节点回滚脚本

### 8.2 回滚后
- [ ] bind mount 已卸载
- [ ] 软链接已删除
- [ ] 原始 `.bak_时间戳` 已恢复为正式路径
- [ ] `/etc/fstab` 本次追加项已清理
- [ ] MySQL/服务已重新启动
- [ ] 业务验证恢复正常

---

## 9. 最终验收检查清单

- [ ] 三台服务器共享盘分区挂载稳定
- [ ] 三套业务系统 MySQL 已落到共享盘
- [ ] 已确认不再写本地 datadir
- [ ] 已完成日志候选清单审核
- [ ] 已迁移的宿主机业务日志正常写入共享盘
- [ ] 已迁移的系统日志正常写入共享盘
- [ ] Node2 未迁移 Docker 容器日志
- [ ] 所有 `.bak_时间戳` 已保留待观察
- [ ] 已记录实施时间、验证结果、异常处理结果
- [ ] 已确认进入观察期

---

## 10. 观察期后清理检查

- [ ] 已连续观察至少 1 个业务高峰周期
- [ ] 未再触发回滚
- [ ] MySQL 与日志均稳定
- [ ] 已确认可以删除本地 `.bak_时间戳` 目录
- [ ] 删除动作已单独审批

---

## 12. 脚本总览

### 12.1 初始化脚本
- `node1_01_init_shared_disk.sh`
- `node2_01_init_shared_disk.sh`
- `node3_01_init_shared_disk.sh`

### 12.2 MySQL 迁移脚本
- `node1_02_migrate_mysql.sh`
- `node2_02_migrate_mysql.sh`
- `node3_02_migrate_mysql.sh`

### 12.3 日志发现脚本
- `node1_03_discover_logs.sh`
- `node2_03_discover_logs.sh`
- `node3_03_discover_logs.sh`

### 12.4 日志迁移脚本
- `node1_04_apply_logs_migration.sh`
- `node2_04_apply_logs_migration.sh`
- `node3_04_apply_logs_migration.sh`

### 12.5 回滚脚本
- `node1_99_rollback_all.sh`
- `node2_99_rollback_all.sh`
- `node3_99_rollback_all.sh`

### 12.6 公共库与自检
- `lib/common.sh`
- `lib/validate.sh`
- `selfcheck.sh`

---

## 13. 脚本源码（完整内嵌）

## 13.1 scripts/lib/common.sh
```bash
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

log() { printf '[%s] %s\n' "$(date '+%F %T')" "$*"; }
fail() { log "ERROR: $*"; exit 1; }
warn() { log "WARN: $*"; }
require_root() { [ "$(id -u)" -eq 0 ] || fail "请使用 root 执行"; }
require_cmd() { command -v "$1" >/dev/null 2>&1 || fail "缺少命令: $1"; }

DRY_RUN=0
RUN_LOG_FILE=""

parse_dry_run_flag() {
  for arg in "$@"; do
    case "$arg" in
      --dry-run) DRY_RUN=1 ;;
    esac
  done
}

run_cmd() {
  if [ "$DRY_RUN" -eq 1 ]; then
    log "[DRY-RUN] $*"
  else
    eval "$@"
  fi
}

ensure_parent_dir() {
  local file="$1"
  mkdir -p "$(dirname "$file")"
}

setup_run_log() {
  local file="$1"
  RUN_LOG_FILE="$file"
  ensure_parent_dir "$file"
  touch "$file"
}

append_run_log() {
  [ -n "$RUN_LOG_FILE" ] || return 0
  printf '[%s] %s\n' "$(date '+%F %T')" "$*" >> "$RUN_LOG_FILE"
}

log_run() {
  log "$*"
  append_run_log "$*"
}

mount_is_bound_to() {
  local source="$1"
  local target="$2"
  mount | grep -F " on $target " | grep -F "$source" >/dev/null 2>&1
}

ensure_not_mounted() {
  local path="$1"
  if mountpoint -q "$path"; then
    fail "$path 当前已挂载，请先检查现场状态"
  fi
}

safe_append_fstab_bind() {
  local source="$1"
  local target="$2"
  local fstab_file="$3"
  local line="$source  $target  none  bind  0 0"
  grep -Fq "$line" "$fstab_file" 2>/dev/null || printf '%s\n' "$line" >> "$fstab_file"
}

safe_remove_fstab_bind() {
  local source="$1"
  local target="$2"
  local fstab_file="$3"
  local tmp_file
  tmp_file="${fstab_file}.tmp.$$"
  if [ ! -f "$fstab_file" ]; then
    return 0
  fi
  grep -Fvx "$source  $target  none  bind  0 0" "$fstab_file" > "$tmp_file" || true
  cat "$tmp_file" > "$fstab_file"
  rm -f "$tmp_file"
}

state_file_header() {
  local state_file="$1"
  ensure_parent_dir "$state_file"
  if [ ! -f "$state_file" ] || [ ! -s "$state_file" ]; then
    printf 'category\tmethod\torig\ttarget\tbackup\n' > "$state_file"
  fi
}

append_state() {
  local state_file="$1"
  local category="$2"
  local method="$3"
  local orig="$4"
  local target="$5"
  local backup="$6"
  printf '%s\t%s\t%s\t%s\t%s\n' "$category" "$method" "$orig" "$target" "$backup" >> "$state_file"
}

wait_for_pid_match() {
  local pattern="$1"
  local retries="${2:-12}"
  local sleep_seconds="${3:-5}"
  local i=1
  while [ "$i" -le "$retries" ]; do
    if pgrep -f "$pattern" >/dev/null 2>&1; then
      return 0
    fi
    sleep "$sleep_seconds"
    i=$((i+1))
  done
  return 1
}

wait_for_port_listen() {
  local port="$1"
  local retries="${2:-12}"
  local sleep_seconds="${3:-5}"
  local i=1
  while [ "$i" -le "$retries" ]; do
    if ss -lntp 2>/dev/null | grep -E ":${port}[[:space:]]" >/dev/null 2>&1; then
      return 0
    fi
    sleep "$sleep_seconds"
    i=$((i+1))
  done
  return 1
}
```

## 13.2 scripts/lib/validate.sh
```bash
#!/usr/bin/env bash
set -euo pipefail

DANGEROUS_ROOTS=(/ /var /opt /usr /data /usr/local /etc /bin /sbin /lib /lib64)

validate_existing_path() {
  local path="$1"
  [ -e "$path" ] || return 1
}

validate_not_dangerous_root() {
  local path="$1"
  for item in "${DANGEROUS_ROOTS[@]}"; do
    [ "$path" = "$item" ] && return 1
  done
  return 0
}

validate_tsv_header() {
  local file="$1"
  local header
  header=$(head -n 1 "$file")
  [ "$header" = $'approved\ttype\tpath\tservice\tdescription\ttarget_subdir\tmethod' ]
}

validate_method() {
  case "$1" in
    bind|symlink) return 0 ;;
    *) return 1 ;;
  esac
}

validate_type() {
  case "$1" in
    dir|file) return 0 ;;
    *) return 1 ;;
  esac
}

validate_no_parent_child_conflicts() {
  local approved_file="$1"
  local tmp
  tmp=$(mktemp)
  tail -n +2 "$approved_file" | awk -F'\t' '$1=="Y" {print $3}' | sort > "$tmp"
  while IFS= read -r path1; do
    [ -n "$path1" ] || continue
    while IFS= read -r path2; do
      [ -n "$path2" ] || continue
      [ "$path1" = "$path2" ] && continue
      case "$path2" in
        "$path1"/*)
          rm -f "$tmp"
          return 1
          ;;
      esac
    done < "$tmp"
  done < "$tmp"
  rm -f "$tmp"
  return 0
}

validate_node2_not_docker_path() {
  local path="$1"
  case "$path" in
    /var/lib/docker/*|*/docker/overlay2/*|*/containers/*) return 1 ;;
    *) return 0 ;;
  esac
}
```

## 13.3 scripts/node1_01_init_shared_disk.sh
```bash
#!/usr/bin/env bash
set -euo pipefail

NODE_NAME="node1"
DEVICE_PART="/dev/sdb1"
MOUNT_POINT="/filedata/node1"
LABEL_NAME="FILEDATA_NODE1"
FS_TYPE="ext4"
BASE_DIR="$MOUNT_POINT"

log() { printf '[%s] %s\n' "$(date '+%F %T')" "$*"; }
fail() { log "ERROR: $*"; exit 1; }
require_root() { [ "$(id -u)" -eq 0 ] || fail "请使用 root 执行"; }
require_cmd() { command -v "$1" >/dev/null 2>&1 || fail "缺少命令: $1"; }

require_root
require_cmd blkid
require_cmd mount
require_cmd mkdir
require_cmd awk

mkdir -p "$MOUNT_POINT"

if ! blkid "$DEVICE_PART" >/dev/null 2>&1; then
  fail "$DEVICE_PART 不存在或未格式化，请先在 node3 上完成分区与 mkfs"
fi

CURRENT_LABEL=$(blkid -s LABEL -o value "$DEVICE_PART" || true)
if [ "$CURRENT_LABEL" != "$LABEL_NAME" ]; then
  log "警告：$DEVICE_PART label 当前为 '$CURRENT_LABEL'，期望 '$LABEL_NAME'"
fi

if mountpoint -q "$MOUNT_POINT"; then
  log "$MOUNT_POINT 已挂载，跳过 mount"
else
  mount "$DEVICE_PART" "$MOUNT_POINT"
  log "已挂载 $DEVICE_PART -> $MOUNT_POINT"
fi

mkdir -p \
  "$BASE_DIR/mysql/mysql57_3603" \
  "$BASE_DIR/logs/services" \
  "$BASE_DIR/logs/system" \
  "$BASE_DIR/meta/manifests" \
  "$BASE_DIR/meta/rollback" \
  "$BASE_DIR/meta/logs"

chmod 755 "$BASE_DIR" "$BASE_DIR/mysql" "$BASE_DIR/logs" "$BASE_DIR/meta"
chown -R root:root "$BASE_DIR/meta"

UUID=$(blkid -s UUID -o value "$DEVICE_PART")
cat > /tmp/node1.fstab.sample <<EOF
UUID=$UUID  $MOUNT_POINT  $FS_TYPE  defaults,noatime,nodiratime  0 2
EOF

log "初始化完成"
log "fstab 示例已生成：/tmp/node1.fstab.sample"
log "请将示例内容并入 /etc/fstab 后执行 mount -a 验证"
```

## 13.4 scripts/node1_02_migrate_mysql.sh
```bash
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
source "$SCRIPT_DIR/lib/common.sh"

ORIG_DATADIR="/data/mysql57/data"
TARGET_DATADIR="/filedata/node1/mysql/mysql57_3603"
MYSQL_USER="mysql"
MYSQL_GROUP="mysql"
MYSQL_BIN="/usr/local/mysql/bin/mysqld"
MYSQL_SAFE_BIN="/usr/local/mysql/bin/mysqld_safe"
MYSQL_PORT="3603"
MYSQL_PID_PATTERN="/usr/local/mysql/bin/mysqld"
META_DIR="/filedata/node1/meta"
STATE_FILE="$META_DIR/manifests/migration_state.tsv"
FSTAB_FILE="/etc/fstab"
TS="$(date '+%Y%m%d%H%M%S')"
BACKUP_DIR="${ORIG_DATADIR}.bak_${TS}"

parse_dry_run_flag "$@"
require_root
require_cmd rsync
require_cmd mount
require_cmd ss
require_cmd pgrep
state_file_header "$STATE_FILE"
setup_run_log "$META_DIR/logs/node1_mysql_migrate_${TS}.log"

[ -d "$ORIG_DATADIR" ] || fail "原始 datadir 不存在: $ORIG_DATADIR"
ensure_not_mounted "$TARGET_DATADIR"
mkdir -p "$TARGET_DATADIR"

log_run "停止 MySQL 进程"
run_cmd "pkill -f '$MYSQL_BIN' || true"
run_cmd "pkill -f '$MYSQL_SAFE_BIN' || true"
sleep 5
if [ "$DRY_RUN" -eq 0 ] && pgrep -f "$MYSQL_PID_PATTERN" >/dev/null 2>&1; then
  fail "MySQL 停止失败，拒绝继续"
fi

log_run "同步数据到共享盘"
run_cmd "rsync -aHAX --numeric-ids '$ORIG_DATADIR/' '$TARGET_DATADIR/'"
run_cmd "chown -R '$MYSQL_USER:$MYSQL_GROUP' '$TARGET_DATADIR'"

log_run "备份原目录并挂载 bind"
run_cmd "mv '$ORIG_DATADIR' '$BACKUP_DIR'"
run_cmd "mkdir -p '$ORIG_DATADIR'"
run_cmd "chown '$MYSQL_USER:$MYSQL_GROUP' '$ORIG_DATADIR'"
run_cmd "mount --bind '$TARGET_DATADIR' '$ORIG_DATADIR'"
if [ "$DRY_RUN" -eq 0 ]; then
  mount_is_bound_to "$TARGET_DATADIR" "$ORIG_DATADIR" || fail "bind mount 校验失败"
  safe_append_fstab_bind "$TARGET_DATADIR" "$ORIG_DATADIR" "$FSTAB_FILE"
  append_state "$STATE_FILE" "mysql" "bind" "$ORIG_DATADIR" "$TARGET_DATADIR" "$BACKUP_DIR"
fi

log_run "启动 MySQL"
run_cmd "nohup '$MYSQL_SAFE_BIN' --datadir='$ORIG_DATADIR' --pid-file='$ORIG_DATADIR/$(hostname).novalocal.pid' >/tmp/node1_mysql_start.log 2>&1 &"
if [ "$DRY_RUN" -eq 0 ]; then
  wait_for_pid_match "$MYSQL_PID_PATTERN" 12 5 || fail "MySQL 进程未拉起"
  wait_for_port_listen "$MYSQL_PORT" 12 5 || fail "MySQL 端口 $MYSQL_PORT 未监听"
  mount_is_bound_to "$TARGET_DATADIR" "$ORIG_DATADIR" || fail "启动后 bind mount 丢失"
fi

log_run "node1 MySQL 迁移完成"
```

## 13.5 scripts/node1_03_discover_logs.sh
```bash
#!/usr/bin/env bash
set -euo pipefail

BASE_DIR="/filedata/node1"
OUT_FILE="$BASE_DIR/meta/manifests/discovered_logs.tsv"

log() { printf '[%s] %s\n' "$(date '+%F %T')" "$*"; }
require_root() { [ "$(id -u)" -eq 0 ] || { echo '请使用 root 执行'; exit 1; }; }

describe_path() {
  case "$1" in
    /var/log) echo "系统日志根目录" ;;
    /var/log/messages) echo "CentOS 综合系统日志" ;;
    /var/log/secure) echo "安全认证日志" ;;
    /var/log/cron) echo "定时任务日志" ;;
    /var/log/audit) echo "审计日志" ;;
    *nginx*) echo "Nginx 访问/错误日志" ;;
    *tomcat*|*catalina*) echo "Tomcat 运行日志" ;;
    *nohup.out) echo "Java/脚本后台输出日志" ;;
    *) echo "待人工识别日志用途" ;;
  esac
}

require_root
mkdir -p "$(dirname "$OUT_FILE")"
printf 'approved\ttype\tpath\tservice\tdescription\ttarget_subdir\tmethod\n' > "$OUT_FILE"

for p in /var/log /var/log/messages /var/log/secure /var/log/cron /var/log/audit; do
  [ -e "$p" ] || continue
  t="dir"; [ -f "$p" ] && t="file"
  printf 'N\t%s\t%s\tsystem\t%s\tsystem/%s\t%s\n' "$t" "$p" "$(describe_path "$p")" "$(basename "$p")" "$( [ "$t" = dir ] && echo bind || echo symlink )" >> "$OUT_FILE"
done

find /opt /data /usr/local -xdev \( -type d \( -iname log -o -iname logs \) -o -type f \( -iname '*.log' -o -name 'nohup.out' -o -name 'catalina.out' \) \) 2>/dev/null | while read -r p; do
  case "$p" in
    /var/lib/docker/*) continue ;;
  esac
  t="dir"; [ -f "$p" ] && t="file"
  svc=$(echo "$p" | awk -F/ '{print $3}')
  [ -n "$svc" ] || svc="misc"
  printf 'N\t%s\t%s\t%s\t%s\tservices/%s\t%s\n' "$t" "$p" "$svc" "$(describe_path "$p")" "$svc" "$( [ "$t" = dir ] && echo bind || echo symlink )" >> "$OUT_FILE"
done

log "已生成 $OUT_FILE，请复制为 approved_logs.tsv 后人工审核 approved 列"
```

## 13.6 scripts/node1_04_apply_logs_migration.sh
```bash
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
source "$SCRIPT_DIR/lib/common.sh"
source "$SCRIPT_DIR/lib/validate.sh"

BASE_DIR="/filedata/node1"
APPROVED_FILE="$BASE_DIR/meta/manifests/approved_logs.tsv"
STATE_FILE="$BASE_DIR/meta/manifests/migration_state.tsv"
FSTAB_FILE="/etc/fstab"
TS="$(date '+%Y%m%d%H%M%S')"
META_LOG_DIR="$BASE_DIR/meta/logs"

parse_dry_run_flag "$@"
require_root
require_cmd rsync
require_cmd mount
state_file_header "$STATE_FILE"
setup_run_log "$META_LOG_DIR/node1_logs_apply_${TS}.log"

[ -f "$APPROVED_FILE" ] || fail "未找到 $APPROVED_FILE"
validate_tsv_header "$APPROVED_FILE" || fail "approved_logs.tsv 表头不合法"
validate_no_parent_child_conflicts "$APPROVED_FILE" || fail "approved_logs.tsv 存在父子路径冲突"

apply_one() {
  local type="$1" path="$2" target_subdir="$3" method="$4"
  local target="$BASE_DIR/logs/$target_subdir"
  local backup="${path}.bak_${TS}"
  local owner

  validate_type "$type" || fail "type 非法: $type"
  validate_method "$method" || fail "method 非法: $method"
  validate_existing_path "$path" || { log_run "跳过不存在路径: $path"; return 0; }
  validate_not_dangerous_root "$path" || fail "危险路径禁止迁移: $path"

  owner=$(stat -c '%U:%G' "$path")
  mkdir -p "$(dirname "$target")"

  if [ "$type" = "dir" ] && [ "$method" = "bind" ]; then
    log_run "迁移目录日志 $path -> $target"
    run_cmd "mkdir -p '$target'"
    run_cmd "rsync -a '$path/' '$target/'"
    run_cmd "chown -R '$owner' '$target'"
    run_cmd "mv '$path' '$backup'"
    run_cmd "mkdir -p '$path'"
    run_cmd "chown '$owner' '$path'"
    run_cmd "mount --bind '$target' '$path'"
    if [ "$DRY_RUN" -eq 0 ]; then
      mount_is_bound_to "$target" "$path" || fail "日志目录 bind mount 校验失败: $path"
      safe_append_fstab_bind "$target" "$path" "$FSTAB_FILE"
      append_state "$STATE_FILE" "log" "bind" "$path" "$target" "$backup"
    fi
  else
    log_run "迁移单文件日志 $path -> $target/$(basename "$path")"
    run_cmd "mkdir -p '$target'"
    run_cmd "cp -a '$path' '$target/$(basename "$path")'"
    run_cmd "mv '$path' '$backup'"
    run_cmd "ln -s '$target/$(basename "$path")' '$path'"
    if [ "$DRY_RUN" -eq 0 ]; then
      [ -L "$path" ] || fail "单文件日志软链接校验失败: $path"
      append_state "$STATE_FILE" "log" "symlink" "$path" "$target/$(basename "$path")" "$backup"
    fi
  fi
}

tail -n +2 "$APPROVED_FILE" | while IFS=$'\t' read -r approved type path service description target_subdir method; do
  [ "$approved" = "Y" ] || continue
  apply_one "$type" "$path" "$target_subdir" "$method"
done

log_run "node1 日志迁移完成"
```

## 13.7 scripts/node1_99_rollback_all.sh
```bash
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
source "$SCRIPT_DIR/lib/common.sh"

STATE_FILE="/filedata/node1/meta/manifests/migration_state.tsv"
FSTAB_FILE="/etc/fstab"
ONLY_CATEGORY="${1:-all}"

require_root
[ -f "$STATE_FILE" ] || fail "未找到 $STATE_FILE"

rollback_one() {
  local category="$1" method="$2" orig="$3" target="$4" backup="$5"
  if [ "$ONLY_CATEGORY" != "all" ] && [ "$ONLY_CATEGORY" != "$category" ]; then
    return 0
  fi
  if mountpoint -q "$orig"; then
    umount "$orig" || fail "卸载失败: $orig"
  fi
  if [ -L "$orig" ]; then
    rm -f "$orig"
  fi
  if [ -e "$orig" ] && [ ! -L "$orig" ]; then
    rm -rf "$orig"
  fi
  if [ -e "$backup" ]; then
    mv "$backup" "$orig"
  else
    warn "备份不存在，无法恢复: $backup"
  fi
  if [ "$method" = "bind" ]; then
    safe_remove_fstab_bind "$target" "$orig" "$FSTAB_FILE"
  fi
}

tac "$STATE_FILE" | tail -n +2 | while IFS=$'\t' read -r category method orig target backup; do
  [ -n "$orig" ] || continue
  rollback_one "$category" "$method" "$orig" "$target" "$backup"
done

log "node1 回滚完成"
```

## 13.8 scripts/node1.fstab.sample
```fstab
UUID=<NODE1_PART_UUID>  /filedata/node1  ext4  defaults,noatime,nodiratime  0 2
/filedata/node1/mysql/mysql57_3603  /data/mysql57/data  none  bind  0 0
```

## 13.9 scripts/node2_01_init_shared_disk.sh
```bash
#!/usr/bin/env bash
set -euo pipefail

NODE_NAME="node2"
DEVICE_PART="/dev/sdb2"
MOUNT_POINT="/filedata/node2"
LABEL_NAME="FILEDATA_NODE2"
FS_TYPE="ext4"
BASE_DIR="$MOUNT_POINT"

log() { printf '[%s] %s\n' "$(date '+%F %T')" "$*"; }
fail() { log "ERROR: $*"; exit 1; }
require_root() { [ "$(id -u)" -eq 0 ] || fail "请使用 root 执行"; }
require_cmd() { command -v "$1" >/dev/null 2>&1 || fail "缺少命令: $1"; }

require_root
require_cmd blkid
require_cmd mount
require_cmd mkdir

mkdir -p "$MOUNT_POINT"

if ! blkid "$DEVICE_PART" >/dev/null 2>&1; then
  fail "$DEVICE_PART 不存在或未格式化，请先在 node3 上完成分区与 mkfs"
fi

CURRENT_LABEL=$(blkid -s LABEL -o value "$DEVICE_PART" || true)
if [ "$CURRENT_LABEL" != "$LABEL_NAME" ]; then
  log "警告：$DEVICE_PART label 当前为 '$CURRENT_LABEL'，期望 '$LABEL_NAME'"
fi

if mountpoint -q "$MOUNT_POINT"; then
  log "$MOUNT_POINT 已挂载，跳过 mount"
else
  mount "$DEVICE_PART" "$MOUNT_POINT"
  log "已挂载 $DEVICE_PART -> $MOUNT_POINT"
fi

mkdir -p \
  "$BASE_DIR/mysql/mysql3308" \
  "$BASE_DIR/mysql/weaver3306" \
  "$BASE_DIR/logs/services" \
  "$BASE_DIR/logs/system" \
  "$BASE_DIR/meta/manifests" \
  "$BASE_DIR/meta/rollback" \
  "$BASE_DIR/meta/logs"

chmod 755 "$BASE_DIR" "$BASE_DIR/mysql" "$BASE_DIR/logs" "$BASE_DIR/meta"
chown -R root:root "$BASE_DIR/meta"

UUID=$(blkid -s UUID -o value "$DEVICE_PART")
cat > /tmp/node2.fstab.sample <<EOF
UUID=$UUID  $MOUNT_POINT  $FS_TYPE  defaults,noatime,nodiratime  0 2
EOF

log "初始化完成"
log "fstab 示例已生成：/tmp/node2.fstab.sample"
log "请将示例内容并入 /etc/fstab 后执行 mount -a 验证"
```

## 13.10 scripts/node2_02_migrate_mysql.sh
```bash
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
source "$SCRIPT_DIR/lib/common.sh"

FSTAB_FILE="/etc/fstab"
META_DIR="/filedata/node2/meta"
STATE_FILE="$META_DIR/manifests/migration_state.tsv"
TS="$(date '+%Y%m%d%H%M%S')"

parse_dry_run_flag "$@"
require_root
require_cmd rsync
require_cmd mount
require_cmd ss
require_cmd pgrep
state_file_header "$STATE_FILE"
setup_run_log "$META_DIR/logs/node2_mysql_migrate_${TS}.log"

migrate_one() {
  local name="$1"
  local datadir="$2"
  local target="$3"
  local start_cmd="$4"
  local stop_pattern="$5"
  local pid_pattern="$6"
  local port="$7"
  local backup_dir="${datadir}.bak_${TS}_${name}"
  local owner

  [ -d "$datadir" ] || fail "原始 datadir 不存在: $datadir"
  ensure_not_mounted "$target"
  mkdir -p "$target"
  owner=$(stat -c '%U:%G' "$datadir")

  log_run "停止实例 $name"
  run_cmd "pkill -f '$stop_pattern' || true"
  sleep 5
  if [ "$DRY_RUN" -eq 0 ] && pgrep -f "$pid_pattern" >/dev/null 2>&1; then
    fail "实例 $name 停止失败"
  fi

  log_run "同步实例 $name 数据"
  run_cmd "rsync -aHAX --numeric-ids '$datadir/' '$target/'"
  run_cmd "chown -R '$owner' '$target'"

  log_run "切换实例 $name 到共享盘"
  run_cmd "mv '$datadir' '$backup_dir'"
  run_cmd "mkdir -p '$datadir'"
  run_cmd "chown '$owner' '$datadir'"
  run_cmd "mount --bind '$target' '$datadir'"

  if [ "$DRY_RUN" -eq 0 ]; then
    mount_is_bound_to "$target" "$datadir" || fail "实例 $name bind mount 校验失败"
    safe_append_fstab_bind "$target" "$datadir" "$FSTAB_FILE"
    append_state "$STATE_FILE" "mysql" "bind" "$datadir" "$target" "$backup_dir"
  fi

  log_run "启动实例 $name"
  run_cmd "nohup bash -c \"$start_cmd\" >/tmp/${name}_mysql_start.log 2>&1 &"
  if [ "$DRY_RUN" -eq 0 ]; then
    wait_for_pid_match "$pid_pattern" 12 5 || fail "实例 $name 进程未拉起"
    wait_for_port_listen "$port" 12 5 || fail "实例 $name 端口 $port 未监听"
    mount_is_bound_to "$target" "$datadir" || fail "实例 $name 启动后 bind mount 丢失"
  fi
}

mkdir -p "$META_DIR/manifests" "$META_DIR/rollback" "$META_DIR/logs"
migrate_one "mysql3308" "/opt/mysql/mydata/3308/data" "/filedata/node2/mysql/mysql3308" "/opt/mysql/mysql/base/bin/mysqld_safe --defaults-file=/opt/mysql/my.conf --user=root" "/opt/mysql/mysql/base/bin/mysqld|/opt/mysql/mysql/base/bin/mysqld_safe" "/opt/mysql/mysql/base/bin/mysqld.*3308|/opt/mysql/mysql/base/bin/mysqld_safe.*my.conf|/opt/mysql/mydata/3308/data" "3308"
migrate_one "weaver3306" "/usr/local/weaver_mysql/data" "/filedata/node2/mysql/weaver3306" "/usr/local/weaver_mysql/base/bin/mysqld_safe --datadir=/usr/local/weaver_mysql/data --pid-file=/usr/local/weaver_mysql/data/$(hostname).pid" "/usr/local/weaver_mysql/base/bin/mysqld|/usr/local/weaver_mysql/base/bin/mysqld_safe" "/usr/local/weaver_mysql/base/bin/mysqld|/usr/local/weaver_mysql/base/bin/mysqld_safe|/usr/local/weaver_mysql/data" "3306"

log_run "node2 双 MySQL 迁移完成"
```

## 13.11 scripts/node2_03_discover_logs.sh
```bash
#!/usr/bin/env bash
set -euo pipefail

BASE_DIR="/filedata/node2"
OUT_FILE="$BASE_DIR/meta/manifests/discovered_logs.tsv"

log() { printf '[%s] %s\n' "$(date '+%F %T')" "$*"; }
require_root() { [ "$(id -u)" -eq 0 ] || { echo '请使用 root 执行'; exit 1; }; }

describe_path() {
  case "$1" in
    /var/log) echo "系统日志根目录" ;;
    /var/log/syslog) echo "Ubuntu 综合系统日志" ;;
    /var/log/auth.log) echo "认证安全日志" ;;
    /var/log/kern.log) echo "内核日志" ;;
    *nginx*) echo "Nginx 访问/错误日志" ;;
    *tomcat*|*catalina*) echo "Tomcat 运行日志" ;;
    *nohup.out) echo "Java/脚本后台输出日志" ;;
    *) echo "待人工识别日志用途" ;;
  esac
}

require_root
mkdir -p "$(dirname "$OUT_FILE")"
printf 'approved\ttype\tpath\tservice\tdescription\ttarget_subdir\tmethod\n' > "$OUT_FILE"

for p in /var/log /var/log/syslog /var/log/auth.log /var/log/kern.log; do
  [ -e "$p" ] || continue
  t="dir"; [ -f "$p" ] && t="file"
  printf 'N\t%s\t%s\tsystem\t%s\tsystem/%s\t%s\n' "$t" "$p" "$(describe_path "$p")" "$(basename "$p")" "$( [ "$t" = dir ] && echo bind || echo symlink )" >> "$OUT_FILE"
done

find /opt /data /usr/local -xdev \( -type d \( -iname log -o -iname logs \) -o -type f \( -iname '*.log' -o -name 'nohup.out' -o -name 'catalina.out' \) \) 2>/dev/null | while read -r p; do
  case "$p" in
    /var/lib/docker/*|*/docker/overlay2/*|*/containers/*) continue ;;
  esac
  t="dir"; [ -f "$p" ] && t="file"
  svc=$(echo "$p" | awk -F/ '{print $3}')
  [ -n "$svc" ] || svc="misc"
  printf 'N\t%s\t%s\t%s\t%s\tservices/%s\t%s\n' "$t" "$p" "$svc" "$(describe_path "$p")" "$svc" "$( [ "$t" = dir ] && echo bind || echo symlink )" >> "$OUT_FILE"
done

log "已生成 $OUT_FILE，请复制为 approved_logs.tsv 后人工审核 approved 列"
```

## 13.12 scripts/node2_04_apply_logs_migration.sh
```bash
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
source "$SCRIPT_DIR/lib/common.sh"
source "$SCRIPT_DIR/lib/validate.sh"

BASE_DIR="/filedata/node2"
APPROVED_FILE="$BASE_DIR/meta/manifests/approved_logs.tsv"
STATE_FILE="$BASE_DIR/meta/manifests/migration_state.tsv"
FSTAB_FILE="/etc/fstab"
TS="$(date '+%Y%m%d%H%M%S')"
META_LOG_DIR="$BASE_DIR/meta/logs"

parse_dry_run_flag "$@"
require_root
require_cmd rsync
require_cmd mount
state_file_header "$STATE_FILE"
setup_run_log "$META_LOG_DIR/node2_logs_apply_${TS}.log"

[ -f "$APPROVED_FILE" ] || fail "未找到 $APPROVED_FILE"
validate_tsv_header "$APPROVED_FILE" || fail "approved_logs.tsv 表头不合法"
validate_no_parent_child_conflicts "$APPROVED_FILE" || fail "approved_logs.tsv 存在父子路径冲突"

apply_one() {
  local type="$1" path="$2" target_subdir="$3" method="$4"
  local target="$BASE_DIR/logs/$target_subdir"
  local backup="${path}.bak_${TS}"
  local owner

  validate_type "$type" || fail "type 非法: $type"
  validate_method "$method" || fail "method 非法: $method"
  validate_existing_path "$path" || { log_run "跳过不存在路径: $path"; return 0; }
  validate_not_dangerous_root "$path" || fail "危险路径禁止迁移: $path"
  validate_node2_not_docker_path "$path" || fail "node2 禁止迁移 Docker 路径: $path"

  owner=$(stat -c '%U:%G' "$path")
  mkdir -p "$(dirname "$target")"

  if [ "$type" = "dir" ] && [ "$method" = "bind" ]; then
    log_run "迁移目录日志 $path -> $target"
    run_cmd "mkdir -p '$target'"
    run_cmd "rsync -a '$path/' '$target/'"
    run_cmd "chown -R '$owner' '$target'"
    run_cmd "mv '$path' '$backup'"
    run_cmd "mkdir -p '$path'"
    run_cmd "chown '$owner' '$path'"
    run_cmd "mount --bind '$target' '$path'"
    if [ "$DRY_RUN" -eq 0 ]; then
      mount_is_bound_to "$target" "$path" || fail "日志目录 bind mount 校验失败: $path"
      safe_append_fstab_bind "$target" "$path" "$FSTAB_FILE"
      append_state "$STATE_FILE" "log" "bind" "$path" "$target" "$backup"
    fi
  else
    log_run "迁移单文件日志 $path -> $target/$(basename "$path")"
    run_cmd "mkdir -p '$target'"
    run_cmd "cp -a '$path' '$target/$(basename "$path")'"
    run_cmd "mv '$path' '$backup'"
    run_cmd "ln -s '$target/$(basename "$path")' '$path'"
    if [ "$DRY_RUN" -eq 0 ]; then
      [ -L "$path" ] || fail "单文件日志软链接校验失败: $path"
      append_state "$STATE_FILE" "log" "symlink" "$path" "$target/$(basename "$path")" "$backup"
    fi
  fi
}

tail -n +2 "$APPROVED_FILE" | while IFS=$'\t' read -r approved type path service description target_subdir method; do
  [ "$approved" = "Y" ] || continue
  apply_one "$type" "$path" "$target_subdir" "$method"
done

log_run "node2 日志迁移完成"
```

## 13.13 scripts/node2_99_rollback_all.sh
```bash
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
source "$SCRIPT_DIR/lib/common.sh"

STATE_FILE="/filedata/node2/meta/manifests/migration_state.tsv"
FSTAB_FILE="/etc/fstab"
ONLY_CATEGORY="${1:-all}"

require_root
[ -f "$STATE_FILE" ] || fail "未找到 $STATE_FILE"

rollback_one() {
  local category="$1" method="$2" orig="$3" target="$4" backup="$5"
  if [ "$ONLY_CATEGORY" != "all" ] && [ "$ONLY_CATEGORY" != "$category" ]; then
    return 0
  fi
  if mountpoint -q "$orig"; then
    umount "$orig" || fail "卸载失败: $orig"
  fi
  if [ -L "$orig" ]; then
    rm -f "$orig"
  fi
  if [ -e "$orig" ] && [ ! -L "$orig" ]; then
    rm -rf "$orig"
  fi
  if [ -e "$backup" ]; then
    mv "$backup" "$orig"
  else
    warn "备份不存在，无法恢复: $backup"
  fi
  if [ "$method" = "bind" ]; then
    safe_remove_fstab_bind "$target" "$orig" "$FSTAB_FILE"
  fi
}

tac "$STATE_FILE" | tail -n +2 | while IFS=$'\t' read -r category method orig target backup; do
  [ -n "$orig" ] || continue
  rollback_one "$category" "$method" "$orig" "$target" "$backup"
done

log "node2 回滚完成"
```

## 13.14 scripts/node2.fstab.sample
```fstab
UUID=<NODE2_PART_UUID>  /filedata/node2  ext4  defaults,noatime,nodiratime  0 2
/filedata/node2/mysql/mysql3308   /opt/mysql/mydata/3308/data   none  bind  0 0
/filedata/node2/mysql/weaver3306  /usr/local/weaver_mysql/data  none  bind  0 0
```

## 13.15 scripts/node3_01_init_shared_disk.sh
```bash
#!/usr/bin/env bash
set -euo pipefail

DEVICE="/dev/sdb"
P1="/dev/sdb1"
P2="/dev/sdb2"
P3="/dev/sdb3"
M1="/filedata/node1"
M2="/filedata/node2"
M3="/filedata/node3"

log() { printf '[%s] %s\n' "$(date '+%F %T')" "$*"; }
fail() { log "ERROR: $*"; exit 1; }
require_root() { [ "$(id -u)" -eq 0 ] || fail "请使用 root 执行"; }
require_cmd() { command -v "$1" >/dev/null 2>&1 || fail "缺少命令: $1"; }

require_root
require_cmd parted
require_cmd mkfs.ext4
require_cmd blkid
require_cmd mount
require_cmd umount

log "高风险操作：该脚本会重建 $DEVICE 分区表"
log "执行前请确认旧 /filedata 上无生产有效数据，且 Node1/Node2 未挂载该共享盘"

if mountpoint -q /filedata; then
  umount /filedata || fail "无法卸载 /filedata"
  log "已卸载旧 /filedata"
fi

mkdir -p "$M1" "$M2" "$M3"

parted -s "$DEVICE" mklabel gpt
parted -s "$DEVICE" mkpart primary ext4 1MiB 500GiB
parted -s "$DEVICE" mkpart primary ext4 500GiB 1300GiB
parted -s "$DEVICE" mkpart primary ext4 1300GiB 100%

mkfs.ext4 -F -L FILEDATA_NODE1 "$P1"
mkfs.ext4 -F -L FILEDATA_NODE2 "$P2"
mkfs.ext4 -F -L FILEDATA_NODE3 "$P3"

mount "$P3" "$M3"
log "已挂载 $P3 -> $M3"

mkdir -p \
  "$M3/mysql/mysql57_3603" \
  "$M3/logs/services" \
  "$M3/logs/system" \
  "$M3/meta/manifests" \
  "$M3/meta/rollback" \
  "$M3/meta/logs"

UUID1=$(blkid -s UUID -o value "$P1")
UUID2=$(blkid -s UUID -o value "$P2")
UUID3=$(blkid -s UUID -o value "$P3")

cat > /tmp/node1.fstab.sample <<EOF
UUID=$UUID1  /filedata/node1  ext4  defaults,noatime,nodiratime  0 2
EOF
cat > /tmp/node2.fstab.sample <<EOF
UUID=$UUID2  /filedata/node2  ext4  defaults,noatime,nodiratime  0 2
EOF
cat > /tmp/node3.fstab.sample <<EOF
UUID=$UUID3  /filedata/node3  ext4  defaults,noatime,nodiratime  0 2
EOF

log "分区与 node3 初始化完成"
log "node1/node2/node3 的 fstab 示例位于 /tmp/*.fstab.sample"
```

## 13.16 scripts/node3_02_migrate_mysql.sh
```bash
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
source "$SCRIPT_DIR/lib/common.sh"

ORIG_DATADIR="/data/mysql57/data"
TARGET_DATADIR="/filedata/node3/mysql/mysql57_3603"
MYSQL_USER="mysql"
MYSQL_GROUP="mysql"
MYSQL_BIN="/usr/local/mysql/bin/mysqld"
MYSQL_SAFE_BIN="/usr/local/mysql/bin/mysqld_safe"
MYSQL_PORT="3603"
MYSQL_PID_PATTERN="/usr/local/mysql/bin/mysqld"
META_DIR="/filedata/node3/meta"
STATE_FILE="$META_DIR/manifests/migration_state.tsv"
FSTAB_FILE="/etc/fstab"
TS="$(date '+%Y%m%d%H%M%S')"
BACKUP_DIR="${ORIG_DATADIR}.bak_${TS}"

parse_dry_run_flag "$@"
require_root
require_cmd rsync
require_cmd mount
require_cmd ss
require_cmd pgrep
state_file_header "$STATE_FILE"
setup_run_log "$META_DIR/logs/node3_mysql_migrate_${TS}.log"

[ -d "$ORIG_DATADIR" ] || fail "原始 datadir 不存在: $ORIG_DATADIR"
ensure_not_mounted "$TARGET_DATADIR"
mkdir -p "$TARGET_DATADIR"

log_run "停止 MySQL 进程"
run_cmd "pkill -f '$MYSQL_BIN' || true"
run_cmd "pkill -f '$MYSQL_SAFE_BIN' || true"
sleep 5
if [ "$DRY_RUN" -eq 0 ] && pgrep -f "$MYSQL_PID_PATTERN" >/dev/null 2>&1; then
  fail "MySQL 停止失败，拒绝继续"
fi

log_run "同步数据到共享盘"
run_cmd "rsync -aHAX --numeric-ids '$ORIG_DATADIR/' '$TARGET_DATADIR/'"
run_cmd "chown -R '$MYSQL_USER:$MYSQL_GROUP' '$TARGET_DATADIR'"

log_run "备份原目录并挂载 bind"
run_cmd "mv '$ORIG_DATADIR' '$BACKUP_DIR'"
run_cmd "mkdir -p '$ORIG_DATADIR'"
run_cmd "chown '$MYSQL_USER:$MYSQL_GROUP' '$ORIG_DATADIR'"
run_cmd "mount --bind '$TARGET_DATADIR' '$ORIG_DATADIR'"
if [ "$DRY_RUN" -eq 0 ]; then
  mount_is_bound_to "$TARGET_DATADIR" "$ORIG_DATADIR" || fail "bind mount 校验失败"
  safe_append_fstab_bind "$TARGET_DATADIR" "$ORIG_DATADIR" "$FSTAB_FILE"
  append_state "$STATE_FILE" "mysql" "bind" "$ORIG_DATADIR" "$TARGET_DATADIR" "$BACKUP_DIR"
fi

log_run "启动 MySQL"
run_cmd "nohup '$MYSQL_SAFE_BIN' --datadir='$ORIG_DATADIR' --pid-file='$ORIG_DATADIR/$(hostname).novalocal.pid' >/tmp/node3_mysql_start.log 2>&1 &"
if [ "$DRY_RUN" -eq 0 ]; then
  wait_for_pid_match "$MYSQL_PID_PATTERN" 12 5 || fail "MySQL 进程未拉起"
  wait_for_port_listen "$MYSQL_PORT" 12 5 || fail "MySQL 端口 $MYSQL_PORT 未监听"
  mount_is_bound_to "$TARGET_DATADIR" "$ORIG_DATADIR" || fail "启动后 bind mount 丢失"
fi

log_run "node3 MySQL 迁移完成"
```

## 13.17 scripts/node3_03_discover_logs.sh
```bash
#!/usr/bin/env bash
set -euo pipefail

BASE_DIR="/filedata/node3"
OUT_FILE="$BASE_DIR/meta/manifests/discovered_logs.tsv"

log() { printf '[%s] %s\n' "$(date '+%F %T')" "$*"; }
require_root() { [ "$(id -u)" -eq 0 ] || { echo '请使用 root 执行'; exit 1; }; }

describe_path() {
  case "$1" in
    /var/log) echo "系统日志根目录" ;;
    /var/log/messages) echo "CentOS 综合系统日志" ;;
    /var/log/secure) echo "安全认证日志" ;;
    /var/log/cron) echo "定时任务日志" ;;
    /var/log/audit) echo "审计日志" ;;
    *nginx*) echo "Nginx 访问/错误日志" ;;
    *tomcat*|*catalina*) echo "Tomcat 运行日志" ;;
    *nohup.out) echo "Java/脚本后台输出日志" ;;
    *) echo "待人工识别日志用途" ;;
  esac
}

require_root
mkdir -p "$(dirname "$OUT_FILE")"
printf 'approved\ttype\tpath\tservice\tdescription\ttarget_subdir\tmethod\n' > "$OUT_FILE"

for p in /var/log /var/log/messages /var/log/secure /var/log/cron /var/log/audit; do
  [ -e "$p" ] || continue
  t="dir"; [ -f "$p" ] && t="file"
  printf 'N\t%s\t%s\tsystem\t%s\tsystem/%s\t%s\n' "$t" "$p" "$(describe_path "$p")" "$(basename "$p")" "$( [ "$t" = dir ] && echo bind || echo symlink )" >> "$OUT_FILE"
done

find /opt /data /usr/local -xdev \( -type d \( -iname log -o -iname logs \) -o -type f \( -iname '*.log' -o -name 'nohup.out' -o -name 'catalina.out' \) \) 2>/dev/null | while read -r p; do
  case "$p" in
    /var/lib/docker/*|*/docker/overlay2/*|*/containers/*) continue ;;
  esac
  t="dir"; [ -f "$p" ] && t="file"
  svc=$(echo "$p" | awk -F/ '{print $3}')
  [ -n "$svc" ] || svc="misc"
  printf 'N\t%s\t%s\t%s\t%s\tservices/%s\t%s\n' "$t" "$p" "$svc" "$(describe_path "$p")" "$svc" "$( [ "$t" = dir ] && echo bind || echo symlink )" >> "$OUT_FILE"
done

log "已生成 $OUT_FILE，请复制为 approved_logs.tsv 后人工审核 approved 列"
```

## 13.18 scripts/node3_04_apply_logs_migration.sh
```bash
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
source "$SCRIPT_DIR/lib/common.sh"
source "$SCRIPT_DIR/lib/validate.sh"

BASE_DIR="/filedata/node3"
APPROVED_FILE="$BASE_DIR/meta/manifests/approved_logs.tsv"
STATE_FILE="$BASE_DIR/meta/manifests/migration_state.tsv"
FSTAB_FILE="/etc/fstab"
TS="$(date '+%Y%m%d%H%M%S')"
META_LOG_DIR="$BASE_DIR/meta/logs"

parse_dry_run_flag "$@"
require_root
require_cmd rsync
require_cmd mount
state_file_header "$STATE_FILE"
setup_run_log "$META_LOG_DIR/node3_logs_apply_${TS}.log"

[ -f "$APPROVED_FILE" ] || fail "未找到 $APPROVED_FILE"
validate_tsv_header "$APPROVED_FILE" || fail "approved_logs.tsv 表头不合法"
validate_no_parent_child_conflicts "$APPROVED_FILE" || fail "approved_logs.tsv 存在父子路径冲突"

apply_one() {
  local type="$1" path="$2" target_subdir="$3" method="$4"
  local target="$BASE_DIR/logs/$target_subdir"
  local backup="${path}.bak_${TS}"
  local owner

  validate_type "$type" || fail "type 非法: $type"
  validate_method "$method" || fail "method 非法: $method"
  validate_existing_path "$path" || { log_run "跳过不存在路径: $path"; return 0; }
  validate_not_dangerous_root "$path" || fail "危险路径禁止迁移: $path"

  owner=$(stat -c '%U:%G' "$path")
  mkdir -p "$(dirname "$target")"

  if [ "$type" = "dir" ] && [ "$method" = "bind" ]; then
    log_run "迁移目录日志 $path -> $target"
    run_cmd "mkdir -p '$target'"
    run_cmd "rsync -a '$path/' '$target/'"
    run_cmd "chown -R '$owner' '$target'"
    run_cmd "mv '$path' '$backup'"
    run_cmd "mkdir -p '$path'"
    run_cmd "chown '$owner' '$path'"
    run_cmd "mount --bind '$target' '$path'"
    if [ "$DRY_RUN" -eq 0 ]; then
      mount_is_bound_to "$target" "$path" || fail "日志目录 bind mount 校验失败: $path"
      safe_append_fstab_bind "$target" "$path" "$FSTAB_FILE"
      append_state "$STATE_FILE" "log" "bind" "$path" "$target" "$backup"
    fi
  else
    log_run "迁移单文件日志 $path -> $target/$(basename "$path")"
    run_cmd "mkdir -p '$target'"
    run_cmd "cp -a '$path' '$target/$(basename "$path")'"
    run_cmd "mv '$path' '$backup'"
    run_cmd "ln -s '$target/$(basename "$path")' '$path'"
    if [ "$DRY_RUN" -eq 0 ]; then
      [ -L "$path" ] || fail "单文件日志软链接校验失败: $path"
      append_state "$STATE_FILE" "log" "symlink" "$path" "$target/$(basename "$path")" "$backup"
    fi
  fi
}

tail -n +2 "$APPROVED_FILE" | while IFS=$'\t' read -r approved type path service description target_subdir method; do
  [ "$approved" = "Y" ] || continue
  apply_one "$type" "$path" "$target_subdir" "$method"
done

log_run "node3 日志迁移完成"
```

## 13.19 scripts/node3_99_rollback_all.sh
```bash
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
source "$SCRIPT_DIR/lib/common.sh"

STATE_FILE="/filedata/node3/meta/manifests/migration_state.tsv"
FSTAB_FILE="/etc/fstab"
ONLY_CATEGORY="${1:-all}"

require_root
[ -f "$STATE_FILE" ] || fail "未找到 $STATE_FILE"

rollback_one() {
  local category="$1" method="$2" orig="$3" target="$4" backup="$5"
  if [ "$ONLY_CATEGORY" != "all" ] && [ "$ONLY_CATEGORY" != "$category" ]; then
    return 0
  fi
  if mountpoint -q "$orig"; then
    umount "$orig" || fail "卸载失败: $orig"
  fi
  if [ -L "$orig" ]; then
    rm -f "$orig"
  fi
  if [ -e "$orig" ] && [ ! -L "$orig" ]; then
    rm -rf "$orig"
  fi
  if [ -e "$backup" ]; then
    mv "$backup" "$orig"
  else
    warn "备份不存在，无法恢复: $backup"
  fi
  if [ "$method" = "bind" ]; then
    safe_remove_fstab_bind "$target" "$orig" "$FSTAB_FILE"
  fi
}

tac "$STATE_FILE" | tail -n +2 | while IFS=$'\t' read -r category method orig target backup; do
  [ -n "$orig" ] || continue
  rollback_one "$category" "$method" "$orig" "$target" "$backup"
done

log "node3 回滚完成"
```

## 13.20 scripts/node3.fstab.sample
```fstab
UUID=<NODE3_PART_UUID>  /filedata/node3  ext4  defaults,noatime,nodiratime  0 2
/filedata/node3/mysql/mysql57_3603  /data/mysql57/data  none  bind  0 0
```

## 13.21 scripts/selfcheck.sh
```bash
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
FILES=(
  "$SCRIPT_DIR/lib/common.sh"
  "$SCRIPT_DIR/lib/validate.sh"
  "$SCRIPT_DIR/node1_02_migrate_mysql.sh"
  "$SCRIPT_DIR/node2_02_migrate_mysql.sh"
  "$SCRIPT_DIR/node3_02_migrate_mysql.sh"
  "$SCRIPT_DIR/node1_04_apply_logs_migration.sh"
  "$SCRIPT_DIR/node2_04_apply_logs_migration.sh"
  "$SCRIPT_DIR/node3_04_apply_logs_migration.sh"
  "$SCRIPT_DIR/node1_99_rollback_all.sh"
  "$SCRIPT_DIR/node2_99_rollback_all.sh"
  "$SCRIPT_DIR/node3_99_rollback_all.sh"
)

for f in "${FILES[@]}"; do
  bash -n "$f"
  echo "OK syntax: $f"
done

echo "All syntax checks passed."
```

---

## 14. 使用建议（执行顺序）

### 14.1 第一步：Node3 重分区与初始化
```bash
bash node3_01_init_shared_disk.sh
```

### 14.2 第二步：Node1 / Node2 挂载初始化
```bash
bash node1_01_init_shared_disk.sh
bash node2_01_init_shared_disk.sh
```

### 14.3 第三步：MySQL 先 dry-run，再正式切换
```bash
bash node1_02_migrate_mysql.sh --dry-run
bash node2_02_migrate_mysql.sh --dry-run
bash node3_02_migrate_mysql.sh --dry-run
```

### 14.4 第四步：日志发现
```bash
bash node1_03_discover_logs.sh
bash node2_03_discover_logs.sh
bash node3_03_discover_logs.sh
```

### 14.5 第五步：审核 approved_logs.tsv
仅将明确批准项改为 `Y`

### 14.6 第六步：日志迁移先 dry-run，再正式执行
```bash
bash node1_04_apply_logs_migration.sh --dry-run
bash node2_04_apply_logs_migration.sh --dry-run
bash node3_04_apply_logs_migration.sh --dry-run
```

### 14.7 第七步：异常时分类回滚
```bash
bash node1_99_rollback_all.sh mysql
bash node2_99_rollback_all.sh log
bash node3_99_rollback_all.sh
```

---

## 15. 最终结论

这套单文档交付版已经将：
- 方案
- SOP
- 检查清单
- fstab 示例
- 全部脚本源码
- 风险预案
- 回滚方案

全部合并到一个 Markdown 文件中，可直接用于交付、评审和现场执行。
